# OpenOCD 配置：烧录和调试共用这一份，改调试器/速度只改这里。
#
# 调试器接口。实测本机接的是 Horco CMSIS-DAP（lsusb: faed:4870）。
# 换 J-Link   就把下面两行换成： source [find interface/jlink.cfg]
# 换 ST-Link  就换成：          source [find interface/stlink.cfg]
source [find interface/cmsis-dap.cfg]
# 这个厂商 VID 不在 OpenOCD 默认扫描表里，显式指定，避免"找不到设备"。
cmsis_dap_vid_pid 0xfaed 0x4870

# source [find interface/jlink.cfg]

transport select swd

# 目标芯片：STM32H723 属于 H7 系列
source [find target/stm32h7x.cfg]

# SWD 时钟，单位 kHz。先给保守值，稳定后可往上试 2000 / 4000。
adapter speed 1000
