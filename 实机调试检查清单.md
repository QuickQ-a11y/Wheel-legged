# 轮腿底盘实机调试检查清单

本文档用于轮腿底盘首次上电、机构标定、低力矩测试和闭环调试。调试时按顺序执行，不要跳过机械参数、传感器和安全输出检查。

## 1. 当前安全状态

当前非零输出由三层条件共同控制：

1. `app_config.h` 中的 `APP_CONFIG_CHASSIS_CONTROLLER_OUTPUT_ENABLE`。
2. `chassis_config.c` 中的 `output.joint_enabled` 和 `output.wheel_enabled`。
3. 关节力矩、轮力矩、电流换算和电流限幅必须为有效非零值。

默认配置仍保持安全零输出。调试时不要一次打开全部条件，应先测试单类电机，并使用很小的力矩或电流限幅。

## 2. 闭环前必须确认

### 2.1 关节机械零位

`angle_offset_rad` 必须让实际机构姿态与 `phi1/phi4` 的模型定义对应。

手动摆动腿部，观察：

```text
chassis.leg[0]    左腿
chassis.leg[1]    右腿
```

左右腿处于机械对称姿态时，应满足：

- 左右 `length_m` 基本一致。
- 左右 `phi0_rad` 基本一致。
- 前后主动杆几何角符合实际姿态。
- 缓慢停止后，腿长速度和腿角速度回到接近 0。

零位错误应修改 `angle_offset_rad`，不能通过 `angle_scale` 补偿。

### 2.2 五连杆机械参数

核对 `chassis_config.c` 中：

```text
link1_m
link2_m
link3_m
link4_m
frame_joint_distance_m
```

当前 `frame_joint_distance_m = 0.0f`。如果实机前后髋关节轴并不重合，必须填写实际距离，否则腿长、腿角、速度和 VMC 力矩都会错误。

还需要确认：

- 当前五连杆交点分支与实机腿部构型一致。
- 正常工作区不会越过机构死点。
- `sin(phi2_rad - phi3_rad)` 不会接近 0。
- 实际腿长保持在允许范围内。

### 2.3 轮半径与模型一致性

当前固件配置：

```text
chassis_config.wheel.radius_m = 0.10m
```

当前 `ABK_LQR.m` 使用：

```text
R_w_ac = 0.155 / 2 = 0.0775m
```

两者不一致会影响轮速换算、`dot_s` 状态和 LQR 模型。闭环前必须确认实际有效轮半径，并统一 MATLAB 与固件参数。

### 2.4 控制周期

当前底盘任务周期约为 `2ms`，PID、Kalman 和位移积分仍使用 IMU 提供的约 `1ms dt`。

这会影响：

- PID 积分速度。
- Kalman 状态转移。
- 前进位移积分。
- 腿长和腿角速度相关计算的时间尺度。

正式闭环前应统一为实际底盘控制周期。

### 2.5 K 拟合范围

固件 K 输入限幅范围为：

```text
0.15m ~ 0.35m
```

现有 MATLAB 系数采样范围仍约为：

```text
0.12m ~ 0.31m
```

因此 `0.31m ~ 0.35m` 当前属于外推区。变腿长进入该范围前，应重新生成 K 拟合系数。

## 3. IMU 检查

整车业务层使用右手系：X 车体前方、Y 车体左方、Z 车体上方。

静止水平放置时检查：

```text
chassis.imu.roll_rad
chassis.imu.pitch_rad
chassis.imu.yaw_rad
chassis.imu.gyro_radps[0..2]
chassis.imu.motion_accel_mps2[0..2]
```

预期结果：

- `roll_rad` 和 `pitch_rad` 接近 0。
- 三轴角速度接近 0。
- 去重力后的运动加速度接近 0。
- IMU 静止时没有明显突跳。

方向检查：

- 抬高车头时，确认 pitch 方向符合右手定则。
- 车体向左侧倾斜时，确认 roll 方向。
- 原地向左转时，确认 yaw 和 Z 轴角速度方向。

当前 yaw 没有磁力计或外部航向观测，会发生长期漂移。K 中 yaw 增益较大，启用控制时必须保证目标 yaw 与当前 yaw 对齐，避免瞬间产生较大转向力矩。

## 4. CAN 与设备状态

### 4.1 电机与 CAN 映射

| 电机 | 数组下标 | 总线 | 命令 ID | 反馈 ID |
|---|---:|---|---:|---:|
| 左前髋 | 0 | FDCAN1 | `0x002` | `0x002` |
| 左后髋 | 1 | FDCAN1 | `0x001` | `0x001` |
| 右前髋 | 2 | FDCAN1 | `0x004` | `0x004` |
| 右后髋 | 3 | FDCAN1 | `0x003` | `0x003` |
| 左轮 | 0 | FDCAN2 | `0x200` 字节 0~1 | `0x201` |
| 右轮 | 1 | FDCAN2 | `0x200` 字节 2~3 | `0x202` |

### 4.2 在线状态

在 Watch 中观察：

```text
chassis.dm_motor[0..3].online
chassis.wheel_motor[0..1].online
chassis.can_tx_error_count
chassis.fault_flags
chassis.state_valid
```

要求：

- 六个电机在线状态持续为 1。
- 手动转动一个电机时，只有对应数组项发生变化。
- 反馈位置、速度和力矩没有异常跳变。
- `fault_flags == CHASSIS_FAULT_NONE`。
- 站立控制完整执行后 `state_valid == 1`。

当前 CAN 发送不重发。DM 如果漏掉一次使能特殊帧，可能没有进入 MIT 模式。调试时需要观察 DM 状态，必要时重新切换一次使能。

## 5. 电机零位与极性

### 5.1 DM 关节电机

`angle_scale` 只转换反馈角度和速度：

```c
phi = angle_offset + angle_scale * motor_position;
phi_speed = angle_scale * motor_speed;
```

`torque_scale` 只转换 VMC 几何力矩：

```c
motor_torque = vmc_geometric_torque * torque_scale;
```

修改规则：

- 反馈角和实际力矩方向同时相反：通常同时反转 `angle_scale` 和 `torque_scale`。
- 只有几何角方向错误：只改 `angle_scale`。
- 几何角正确但力矩方向错误：只改 `torque_scale`。
- 机械零位错误：改 `angle_offset_rad`。
- 电机对应关系错误：改 `motor_index` 或 CAN ID。

设备层还通过 `APP_CONFIG_DM_LEFT_DIRECTION` 和 `APP_CONFIG_DM_RIGHT_DIRECTION` 处理左右安装镜像。不要同时随意修改设备层 `direction` 和底盘层两个 scale，避免重复反向。

### 5.2 DJI 轮电机

轮速反馈方向由以下配置控制：

```text
left_speed_scale
right_speed_scale
```

轮力矩到电流使用公共 `torque_to_current`。当前没有左右独立的输出极性系数：

- 两侧都需要反向时，可以统一修改公共换算符号。
- 只有一侧输出方向错误时，需要增加左右独立输出方向配置，不能使用 DM 的 `angle_scale/torque_scale`。

## 6. 腿部几何与状态检查

手动缓慢活动腿部，观察：

```text
chassis.leg[0].phi1_rad
chassis.leg[0].phi2_rad
chassis.leg[0].phi3_rad
chassis.leg[0].phi4_rad
chassis.leg[0].length_m
chassis.leg[0].phi0_rad
chassis.leg[0].length_speed_mps
chassis.leg[0].phi0_speed_radps

chassis.leg[1]
```

检查要求：

- 实际腿长连续变化，没有跳变或突然清零。
- 腿长应落在 `0.15m ~ 0.35m` 的目标工作范围。
- 左右机械对称时，左右腿状态基本对称。
- 静止时速度接近 0。
- 正常工作区不进入几何奇异状态。

如果 `state_valid` 不能置 1，优先检查腿长是否被清零、机构是否无解以及 `phi2-phi3` 是否接近奇异位置。

## 7. 十维状态与速度融合

状态顺序固定为：

```text
0  s
1  dot_s
2  fai
3  dot_fai
4  theta_l
5  dot_theta_l
6  theta_r
7  dot_theta_r
8  theta_b
9  dot_theta_b
```

Watch 变量：

```text
chassis.lqr_state[0..9]
chassis.forward_speed_raw_mps
chassis.forward_speed_mps
chassis.forward_accel_mps2
chassis.forward_accel_fused_mps2
chassis.forward_position_m
```

检查要求：

- 推动车体前进时，`dot_s` 方向正确。
- 静止时融合速度不会持续明显漂移。
- 左右轮同向转动时主要改变前进速度。
- 左右轮反向转动时不应被误算为明显前进速度。
- 左右腿状态进入 `theta_l/theta_r` 的符号符合离线模型。

## 8. K 矩阵与 LQR 输出

Watch 变量：

```text
chassis.k_input_length_m[0..1]
chassis.k_limited_length_m[0..1]
chassis.k_fit_enabled
chassis.k_length_limited
chassis.lqr_k[4][10]
chassis.lqr_output[4]
```

输出顺序：

```text
0  左轮力矩
1  右轮力矩
2  左腿摆力矩
3  右腿摆力矩
```

检查要求：

- 固定腿长调试时，K 输入应为配置的固定腿长。
- 实时腿长模式下，K 输入应跟随左右腿实际长度。
- 正常工作范围内 `k_length_limited` 应为 0。
- 左右对称状态下，左右 K 和输出应满足模型对称关系。
- 状态微小变化不应导致 K 或输出发生不连续跳变。

## 9. PID 与支撑力

当前配置中的主要参数：

```text
腿长 PID kd = 8000
横滚 PID kp = 3000
PID 输出限幅 = 300N
base_support_force_n = -30N
```

Watch 变量：

```text
chassis.support_force_n[0]
chassis.support_force_n[1]
```

检查要求：

- 静止时支撑力稳定，不应高频大幅振荡。
- 左右水平时，两侧支撑力差值合理。
- 车体横滚时，左右支撑力差应向恢复水平的方向变化。
- 支撑力不应长期卡在 PID 输出限幅。
- `base_support_force_n` 的负号必须与当前 VMC 定义一致，确保腿产生正确的地面支撑方向。

腿长速度或 IMU 噪声较大时，当前较大的微分和比例参数可能迅速使支撑力饱和。启用电机前先在安全零输出状态观察这些中间量。

## 10. 最终输出检查

Watch 变量：

```text
chassis.safe_output
chassis.fault_flags
chassis.joint_torque_nm[0..3]
chassis.wheel_current[0..1]
```

注意：输出开关关闭时，最终命令数组保持为 0。此时可先观察 `lqr_output` 和 `support_force_n` 判断控制方向。

打开输出后检查：

- 所有命令均受限幅约束。
- 未使能、设备离线或控制计算失败时立即恢复零输出。
- 没有旧命令残留。
- 单个电机命令只作用于对应电机。
- 电机温度、电流和机械冲击处于安全范围。

## 11. 推荐调试顺序

1. 保持所有输出开关关闭，检查 IMU、CAN、编码器和在线状态。
2. 手动活动左右腿，确认零位、几何角、腿长和速度。
3. 核对五连杆尺寸、轮半径、控制周期和 K 采样范围。
4. 在板凳或悬挂状态逐个 DM 输出很小力矩，确认关节力矩方向。
5. 单独测试左右轮小电流，确认正电流对应的实际轮转方向。
6. 设置很小的关节和轮输出限幅，验证 VMC 映射。
7. 保持固定腿长 K，检查十维状态和四路 LQR 输出符号。
8. 在保护绳、急停和机械限位可靠的条件下进行低增益平衡测试。
9. 确认固定腿长稳定后，再切换实时腿长 K。
10. 最后逐步扩大腿长范围、速度目标和输出限幅。

## 12. 每次上电前检查

- 机器人已架空、使用板凳或安装保护绳。
- 急停和主电源能够立即断开。
- 六个电机 CAN ID 与程序映射一致。
- DM 已进入正确控制模式。
- IMU 已完成初始化、温控和零偏稳定。
- 腿部没有处于机械死点或极限位置。
- 软件处于零力矩模式。
- 所有输出限幅仍为当前调试阶段允许的小值。
- Watch 中 `fault_flags`、`state_valid` 和在线状态正常。
- 现场人员远离轮子、连杆和夹伤区域。
