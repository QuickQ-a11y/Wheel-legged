# 代码相关介绍

本文档面向刚接手本工程的开发者，目标是让你先理解代码框架、模块边界和当前安全策略，再开始改代码。阅读代码前，请先阅读同目录下的 `代码风格要求.md`。

## 1. 工程定位

当前工程是基于 STM32H723 的轮腿底盘实机工程。工程主体仍由 STM32CubeMX 生成，用户代码集中放在 `User_File` 目录。

当前 `User_File` 是一个安全可编译的新框架，重点是把层级和接口先理清楚：

- 驱动层只负责硬件收发。
- 设备层只负责设备协议解析和设备状态维护。
- 底盘模块层负责五连杆、十维 LQR、支撑力、VMC、故障和安全输出。
- 任务层只负责 FreeRTOS 调度、模块连接和数据分发。
- USB CDC 已接入 PA11/PA12，包含 CRC、流式协议解析、接收环形缓冲和批量发送队列。
- 当前已经加入第一阶段轮腿控制器计算链，包括五连杆几何、虚拟腿长/腿角状态、LQR 腿长拟合 K、腿长/横滚虚拟支撑力和 VMC 关节力矩映射。
- 当前不包含可直接启用实机闭环的已验证 LQR 参数、跳跃、功率限制或完整运动闭环；打滑、离地和转向载荷目前只生成 Watch 观测量，不参与控制输出。
- 上电默认安全零输出；第一阶段即使控制器能计算中间量，默认也不会产生非零电机命令。

## 2. 顶层目录关系

工程中和用户代码最相关的文件如下：

```text
Wheel-legged/
├── CMakeLists.txt
├── 代码风格要求.md
├── 代码相关介绍.md
├── Core/
│   ├── Inc/
│   └── Src/
│       ├── main.c
│       ├── freertos.c
│       ├── fdcan.c
│       ├── spi.c
│       └── gpio.c
├── USB_DEVICE/
│   ├── App/
│   └── Target/
├── Middlewares/ST/STM32_USB_Device_Library/
└── User_File/
    ├── 1_Middleware/
    │   ├── 0_Common/
    │   ├── 1_Driver/
    │   └── 2_Algorithm/
    ├── 2_Device/
    ├── 3_Chariot/
    └── 5_Task/
```

### 2.1 `Core`

`Core` 是 CubeMX 生成层，主要提供 MCU、外设和 FreeRTOS 基础入口。

当前用户代码依赖的事实包括：

- `Core/Src/main.c` 初始化 GPIO、FDCAN1、FDCAN2、SPI2 和 FreeRTOS。
- `Core/Src/freertos.c` 在 `MX_FREERTOS_Init()` 中创建默认任务，并调用：
  - `CAN_Task_Init()`
  - `Chassis_Task_Init()`
  - `IMU_Task_Init()`
  - `USB_Task_Init()`
- `Core/Src/fdcan.c` 定义 `hfdcan1` 和 `hfdcan2`。
- `Core/Src/spi.c` 定义 `hspi2`。
- `Core/Inc/main.h` 定义 BMI088 片选和中断引脚：
  - `ACC_CS_Pin` / `ACC_CS_GPIO_Port`
  - `GYRO_CS_Pin` / `GYRO_CS_GPIO_Port`
  - `ACC_INT_Pin`
  - `GYRO_INT_Pin`

除非需要修改外设配置，否则不要直接改 CubeMX 生成代码。用户逻辑应优先放在 `User_File`。

### 2.2 `User_File`

`User_File` 是当前工程的用户代码根目录。它按职责分层：

```text
User_File/
├── 1_Middleware/
│   ├── 0_Common/
│   │   └── app_config.h
│   ├── 1_Driver/
│   │   ├── FDCAN/
│   │   │   ├── driver_fdcan.c
│   │   │   └── driver_fdcan.h
│   │   ├── SPI/
│   │   │   ├── driver_spi.c
│   │   │   └── driver_spi.h
│   │   └── USB/
│   │       ├── driver_usb.c
│   │       └── driver_usb.h
│   └── 2_Algorithm/
│       ├── Kalman.c
│       ├── Kalman.h
│       ├── LQR.c
│       ├── LQR.h
│       ├── PID.c
│       ├── PID.h
│       ├── CRC.c
│       └── CRC.h
├── 2_Device/
│   ├── Communication/USB/
│   │   ├── device_usb_protocol.c
│   │   └── device_usb_protocol.h
│   ├── IMU/BMI088/
│   │   ├── device_bmi088.c
│   │   └── device_bmi088.h
│   └── Motor/
│       ├── Motor_DM/
│       │   ├── device_motor_dm.c
│       │   └── device_motor_dm.h
│       └── Motor_DJI/
│           ├── device_motor_dji.c
│           └── device_motor_dji.h
├── 3_Chariot/
│   └── 1_Module/Chassis/
│       ├── chassis_config.c
│       ├── chassis_config.h
│       ├── chassis_control.c
│       ├── chassis_control.h
│       ├── chassis_vmc.c
│       └── chassis_vmc.h
└── 5_Task/
    ├── task_can.c
    ├── task_can.h
    ├── chassis_task.c
    ├── chassis_task.h
    ├── task_imu.c
    ├── task_imu.h
    ├── task_usb.c
    ├── task_usb.h
    ├── task_can_dispatch.c
    └── task_can_dispatch.h
```

当前框架不再保留独立交互层目录。遥控器、上位机或裁判系统后续按实际职责放入任务层或设备层，避免为了层级而增加中间状态。

## 3. 构建入口

根目录 `CMakeLists.txt` 只直接维护以下用户代码：

```text
User_File/1_Middleware/1_Driver/FDCAN/driver_fdcan.c
User_File/1_Middleware/1_Driver/SPI/driver_spi.c
User_File/1_Middleware/1_Driver/USB/driver_usb.c
User_File/1_Middleware/2_Algorithm/CRC.c
User_File/1_Middleware/2_Algorithm/Kalman.c
User_File/1_Middleware/2_Algorithm/LQR.c
User_File/1_Middleware/2_Algorithm/PID.c
User_File/2_Device/IMU/BMI088/device_bmi088.c
User_File/2_Device/Motor/Motor_DM/device_motor_dm.c
User_File/2_Device/Motor/Motor_DJI/device_motor_dji.c
User_File/2_Device/Communication/USB/device_usb_protocol.c
User_File/3_Chariot/1_Module/Chassis/chassis_config.c
User_File/3_Chariot/1_Module/Chassis/chassis_control.c
User_File/3_Chariot/1_Module/Chassis/chassis_vmc.c
User_File/5_Task/task_can.c
User_File/5_Task/chassis_task.c
User_File/5_Task/task_imu.c
User_File/5_Task/task_can_dispatch.c
User_File/5_Task/task_usb.c
```

CubeMX 生成的 USB Device、USB CDC、PCD 和 USB LL 源文件由 `cmake/stm32cubemx/CMakeLists.txt` 统一维护，不能再重复加入根目录 `CMakeLists.txt`，否则链接阶段会出现 `multiple definition`。

新增 `.c` 文件后，必须同步加入 `CMakeLists.txt` 的 `target_sources()`。新增头文件目录后，必须同步加入 `target_include_directories()`。

当前 include 路径已经包含：

```text
User_File/1_Middleware/0_Common
User_File/1_Middleware/1_Driver/FDCAN
User_File/1_Middleware/1_Driver/SPI
User_File/1_Middleware/1_Driver/USB
User_File/1_Middleware/2_Algorithm
User_File/2_Device/IMU/BMI088
User_File/2_Device/Motor/Motor_DM
User_File/2_Device/Motor/Motor_DJI
User_File/2_Device/Communication/USB
User_File/3_Chariot/1_Module/Chassis
User_File/5_Task
```

## 4. 分层职责总览

当前代码的核心原则是“下层不知道上层业务，上层不直接碰硬件细节”。

### 4.1 公共配置层：`1_Middleware/0_Common`

公共层保存跨模块共享的基础类型和集中配置。

#### `app_config.h`

集中定义硬件映射、任务周期、设备数量和超时阈值。

当前公共接口约定：

- 不使用统一状态返回类型或跨模块状态码枚举。
- 模块公共接口默认返回 `void`，参数非法时直接返回。
- `uint8_t`、`uint32_t` 返回值只用于明确业务查询，例如在线状态、使能状态、故障位、错误码和计数器。
- 驱动层 HAL 失败不向上层转换统一错误码；安全保护依赖设备在线状态、故障位和安全零输出链路。

当前关键配置：

| 配置 | 当前值 | 含义 |
|---|---:|---|
| `APP_CONFIG_CAN_MAX_DATA_LENGTH` | `8U` | Classic CAN 最大数据长度 |
| `APP_CONFIG_CAN_TX_FRAME_CAPACITY` | `16U` | CAN 发送缓存槽数量 |
| `APP_CONFIG_CAN_RX_QUEUE_LENGTH` | `32U` | CAN 接收队列长度 |
| `APP_CONFIG_DM_MOTOR_COUNT` | `4U` | DM 髋关节电机数量 |
| `APP_CONFIG_DJI_WHEEL_COUNT` | `2U` | DJI 轮电机数量 |
| `APP_CONFIG_CAN_TASK_PERIOD_TICKS` | `1U` | CAN 任务周期 |
| `APP_CONFIG_IMU_TASK_PERIOD_TICKS` | `1U` | IMU 任务周期 |
| `APP_CONFIG_CHASSIS_TASK_PERIOD_TICKS` | `2U` | 底盘任务周期 |
| `APP_CONFIG_USB_RX_BUFFER_SIZE` | `2048U` | USB CDC 接收环形缓冲字节数，必须为 2 的幂 |
| `APP_CONFIG_USB_TX_FRAME_CAPACITY` | `32U` | 状态帧发送 FIFO 容量 |
| `APP_CONFIG_USB_TX_BATCH_FRAME_COUNT` | `16U` | 一次 CDC IN 最多合并的 31 字节状态帧数 |
| `APP_CONFIG_USB_TASK_WAIT_TICKS` | `1U` | USB 任务无事件时的最长等待 tick |
| `APP_CONFIG_USB_STATUS_PERIOD_TICKS` | `2U` | USB 状态帧独立发送周期，当前对应 500 Hz |
| `APP_CHASSIS_OUTPUT_ENABLE` | `0U` | 控制器非零输出编译总开关，默认封锁 |
| `APP_CONFIG_DM_ONLINE_TIMEOUT_TICKS` | `50U` | DM 在线超时 |
| `APP_CONFIG_DJI_ONLINE_TIMEOUT_TICKS` | `50U` | DJI 在线超时 |
| `APP_CONFIG_CAN_TX_ERROR_LIMIT` | `1000U` | CAN 发送错误阈值 |

当前硬件映射：

| 设备 | 总线 | ID / 引脚 |
|---|---|---|
| 4 个 DM 髋关节电机 | FDCAN1 | `0x001`、`0x002`、`0x003`、`0x004` |
| 2 个 DJI 轮电机 | FDCAN2 | 反馈 `0x201`、`0x202`，控制 `0x200` |
| BMI088 | SPI2 | 片选来自 `main.h` 的 `ACC_CS` 和 `GYRO_CS` |

### 4.2 公共算法层：`1_Middleware/2_Algorithm`

公共算法层保存可以被多个业务模块复用的纯计算代码。算法层不直接访问 HAL、FreeRTOS、CAN、SPI 或电机设备状态，也不维护底盘业务故障位。

当前已有公共算法：

| 文件 | 职责 |
|---|---|
| `PID.h` / `PID.c` | 提供通用 PID 状态、PID 参数和基于反馈速度阻尼项的 PID 更新接口 |
| `LQR.h` / `LQR.c` | 提供通用 LQR `poly22` 增益拟合接口，按左右腿长计算输出-状态 K 矩阵 |
| `Kalman.h` / `Kalman.c` | 提供固定最大维度、无动态内存分配的通用线性卡尔曼滤波接口 |
| `CRC.h` / `CRC.c` | 提供查表实现的通用 CRC8 和 CRC16，当前供 USB 通信协议复用 |

`PID` 的使用方式：

- 调用方在自己的模块状态中保存 `algorithm_pid_state_t`。
- 调用方在自己的配置中保存 `algorithm_pid_config_t`。
- 公共 PID 只计算输出，不知道输出单位。
- 积分限幅和输出限幅由调用方按实际物理量配置。
- 当前底盘控制器的腿长控制和横滚控制都复用这一套 PID。

`Kalman` 的使用方式：

- 调用方在自己的模块状态中保存 `algorithm_kalman_t`。
- 调用方负责填写状态转移矩阵 `F`、测量矩阵 `H`、过程噪声 `Q`、测量噪声 `R` 和协方差 `P`。
- 公共卡尔曼只执行线性卡尔曼五式，不知道业务状态含义。
- 当前底盘控制器用它融合 `dot_s`，状态为 `[dot_s, ddot_s]`，测量为 `[轮腿几何原始速度, IMU 前向运动加速度]`。

后续如果引入 MPC、低通滤波或其它矩阵运算等可复用算法，应优先放在本目录。不要为了预留结构创建空文件；只有当真实控制链路用到对应算法时，再新增完整实现并接入 `CMakeLists.txt`。

## 5. 驱动层

驱动层位于 `User_File/1_Middleware/1_Driver`。

驱动层只做硬件收发，不做业务解析。比如 FDCAN 驱动不知道 DM 电机和 DJI 电机，SPI 驱动不知道 BMI088 寄存器协议。

### 5.1 FDCAN 驱动：`driver_fdcan`

文件：

- `driver_fdcan.h`
- `driver_fdcan.c`

职责：

- 管理 FDCAN1 和 FDCAN2。
- 配置标准 ID 接收过滤器。
- 启动 FDCAN 外设。
- 发送标准 ID Classic CAN 数据帧。
- 在 HAL 接收回调中读取 FIFO 并转发给上层回调。

主要接口：

| 接口 | 作用 |
|---|---|
| `Driver_FDCAN_Init()` | 初始化 FDCAN 并注册接收回调 |
| `Driver_FDCAN_RegisterRxCallback()` | 更新接收回调 |
| `Driver_FDCAN_SendData()` | 发送一帧标准 ID Classic CAN 数据帧 |
| `Driver_FDCAN_DlcToLength()` | 将 HAL DLC 转为真实长度 |

接收流程：

```text
HAL_FDCAN_RxFifo0Callback()
└── Driver_FDCAN_DispatchRxFifo()
    ├── HAL_FDCAN_GetRxMessage()
    └── rxCallback(handle, frame)
```

驱动层接收回调类型是：

```c
typedef void (*driver_fdcan_rx_callback_t)(FDCAN_HandleTypeDef *handle,
                                        const driver_fdcan_rx_frame_t *frame);
```

当前上层注册的回调是 `CAN_Task_RxCallback()`。

### 5.2 SPI 驱动：`driver_spi`

文件：

- `driver_spi.h`
- `driver_spi.c`

职责：

- 在一次片选有效窗口内阻塞发送 SPI 数据。
- 在一次片选有效窗口内阻塞同步收发 SPI 数据。
- 只负责片选时序和 HAL SPI 调用。

主要接口：

| 接口 | 作用 |
|---|---|
| `Driver_SPI_Transmit()` | 拉低片选，阻塞发送，释放片选 |
| `Driver_SPI_TransmitReceive()` | 拉低片选，阻塞同步收发，释放片选 |

SPI 驱动不解释寄存器，不知道 BMI088 的 dummy 字节规则。BMI088 协议由设备层处理。

### 5.3 USB CDC 驱动：`driver_usb`

USB 硬件和 STM32 USB Device 生成代码位于：

- `USB_DEVICE/App`
- `USB_DEVICE/Target`
- `Middlewares/ST/STM32_USB_Device_Library`
- `Drivers/STM32H7xx_HAL_Driver` 中的 PCD/USB LL 文件

用户驱动文件为 `driver_usb.h` 和 `driver_usb.c`。该层只封装 CDC 数据提交、OUT 数据回调、IN 完成回调和断开事件，不解析 SOF、命令号或 CRC。

当前硬件配置：

| 项目 | 配置 |
|---|---|
| USB 外设 | `USB_OTG_HS` |
| PHY | Internal Full Speed PHY |
| 引脚 | PA11/USB_DM、PA12/USB_DP |
| USB 时钟 | HSI48，48 MHz |
| USB DMA | 关闭 |
| VBUS sensing | 关闭 |
| OTG_HS 中断优先级 | 6，可安全调用 FreeRTOS ISR 接口 |
| 硬件 Rx FIFO | `0x200` words |
| 硬件 Tx FIFO0 | `0x80` words |
| 硬件 Tx FIFO1 | `0x174` words |

内部 Full Speed PHY 不使用 USB DMA。接收回调只把数据交给任务层环形缓冲，协议解析不在 USB 中断中执行。

当前 `Wheel-legged.ioc` 尚未保存 USB 配置。下一次用 CubeMX 重新生成代码前，必须先手动完成以下配置，否则生成结果会覆盖当前手工接入内容：

1. 在 Connectivity 中启用 `USB_OTG_HS`，选择内部 Full Speed PHY 和 `Device Only` 模式。
2. 确认 PA11 为 `USB_OTG_HS_DM`，PA12 为 `USB_OTG_HS_DP`。
3. 在 Middleware and Software Packs 中启用 `USB_DEVICE`，HS 接口类别选择 CDC。
4. 在 RCC/Clock Configuration 中开启 HSI48，并将 USB 时钟源选择为 HSI48，频率必须为 48 MHz。
5. USB 参数中关闭 DMA、VBUS sensing、SOF、Low Power、LPM、Dedicated EP1 和 External VBUS。
6. 在 NVIC 中启用 `OTG_HS global interrupt`，抢占优先级设置为 6。
7. 重新生成后检查 `APP_RX_DATA_SIZE` 和 `APP_TX_DATA_SIZE` 均为 2048，并保留 `usbd_conf.c` 中的三个硬件 FIFO 数值。

## 6. 设备层

设备层位于 `User_File/2_Device`。

设备层负责具体设备的协议解析、单位换算和状态维护。设备层不创建任务，不直接决定控制策略。

### 6.1 BMI088：`device_bmi088`

文件：

- `device_bmi088.h`
- `device_bmi088.c`

职责：

- 初始化 BMI088 加速度计和陀螺仪。
- 配置量程、输出频率和数据就绪中断。
- 阻塞读取加速度、角速度和温度。
- 将原始值换算为工程单位。
- 保存最近一次错误码。

当前配置：

| 部分 | 配置 |
|---|---|
| 加速度计 | `6G / 1600Hz` |
| 陀螺仪 | `2000dps / 230Hz` |
| 加速度单位 | `m/s^2` |
| 角速度单位 | `rad/s` |
| 温度单位 | 摄氏度 |

主要接口：

| 接口 | 作用 |
|---|---|
| `BMI088_Init()` | 初始化 BMI088 |
| `BMI088_Read()` | 读取一次完整 IMU 数据 |
| `BMI088_GetErrorCode()` | 获取最近错误码 |

注意事项：

- ACC 读寄存器协议是“读命令 + 1 字节 dummy + 数据”。
- GYRO 读寄存器协议是“读命令 + 数据”。
- 这两种时序差异在 `device_bmi088.c` 内处理，外部不需要关心。

### 6.2 DM 电机：`device_motor_dm`

文件：

- `device_motor_dm.h`
- `device_motor_dm.c`

职责：

- 管理 4 个 DM 髋关节电机。
- 保存每个电机的 CAN 总线、控制器 ID 和安装方向。
- 解析 DM MIT 反馈。
- 按Master ID接收反馈，再通过`D[0]`中的控制器ID分流四台电机。
- 将单圈位置反馈连续展开，并在掉线后重新建立基准。
- 按 SPR 工程写法将 DM MIT 力矩命令打包到 CAN 发送缓存。
- 维护安全输出、使能状态和在线状态。

当前 4 个 DM 电机索引：

```c
MOTOR_DM_LEFT_FRONT
MOTOR_DM_LEFT_BACK
MOTOR_DM_RIGHT_FRONT
MOTOR_DM_RIGHT_BACK
```

当前映射：

| 电机 | 总线 | 命令 ID | 反馈CAN ID | `D[0]`电机ID | 方向 |
|---|---|---:|---:|---:|---:|
| 左前髋关节 | FDCAN1 | `0x002` | `0x000` | `0x2` | `1` |
| 左后髋关节 | FDCAN1 | `0x001` | `0x000` | `0x1` | `1` |
| 右前髋关节 | FDCAN1 | `0x004` | `0x000` | `0x4` | `-1` |
| 右后髋关节 | FDCAN1 | `0x003` | `0x000` | `0x3` | `-1` |

DM MIT 力矩命令字段：

| 字段 | 单位 | 当前安全输出 |
|---|---|---:|
| `torqueNm` | N*m | `0` |

当前发送帧采用 SPR 工程的简化力矩写法：

| 字节 | 内容 |
|---:|---|
| `data[0]` ~ `data[5]` | 固定为 `0x00` |
| `data[6]`高4位 | `Kd[3:0]`，固定为0 |
| `data[6]`低4位 | `T[11:8]` |
| `data[7]` | `T[7:0]` |

该写法只使用 DM MIT 的力矩通道，发送层不会下发位置、速度、KP、KD。

当前临时映射参数为`PMAX=pi rad`、`VMAX=30 rad/s`、`TMAX=10 N*m`，必须与电机调试助手中的控制幅值一致。`positionWrappedRad`保存单圈角，`positionRad`保存本次连续在线期间按`2*pi`展开后的关节角。

主要接口：

| 接口 | 作用 |
|---|---|
| `Motor_DM_Init()` | 初始化 4 个电机配置和状态 |
| `Motor_DM_UpdateFeedback()` | 解析一帧 DM 反馈 |
| `Motor_DM_SetCommand()` | 设置单个电机命令缓存 |
| `Motor_DM_GetState()` | 读取单个电机状态 |
| `Motor_DM_SetSafe()` | 设置安全输出标志 |
| `Motor_DM_SetEnable()` | 请求 DM 使能或失能特殊帧 |
| `Motor_DM_IsOnline()` | 根据反馈 tick 判断在线 |
| `Motor_DM_ZeroAll()` | 清空所有命令 |
| `Motor_DM_UpdateTxFrames()` | 将命令打包到 CAN 发送缓存 |

安全规则：

- `dmSafe = 1` 时，`Motor_DM_UpdateTxFrames()` 会强制把命令清零后再打包。
- 底盘任务上电后请求DM协议使能以取得反馈，协议使能期间仍受`dmSafe`零力矩保护。
- `Motor_DM_ZeroAll()` 只清命令缓存，不直接发 CAN。
- 真正写入 CAN 发送缓存的是 `Motor_DM_UpdateTxFrames()`。
- 真正触发 CAN 发送的是 `CAN_Task_RequestTx()` 或 CAN 任务周期发送。

### 6.3 DJI 电机：`device_motor_dji`

文件：

- `device_motor_dji.h`
- `device_motor_dji.c`

职责：

- 解析 DJI ESC 反馈帧。
- 保存编码器、速度、电流、温度、反馈次数和在线状态。

反馈帧格式：

| 字节 | 含义 |
|---|---|
| `0..1` | 编码器原始值，大端 |
| `2..3` | 转速 `rpm`，大端有符号 |
| `4..5` | 电流原始值，大端有符号 |
| `6` | 温度，摄氏度 |
| `7` | 当前未使用 |

主要接口：

| 接口 | 作用 |
|---|---|
| `Motor_DJI_UpdateFeedback()` | 解析一帧 DJI 反馈 |
| `Motor_DJI_IsOnline()` | 根据反馈 tick 判断在线 |

DJI 电机的控制发送不在设备层完成，而由 `CAN_Task_SetDjiCurrent()` 打包 `0x200` 控制帧。

### 6.4 USB 通信协议：`device_usb_protocol`

该模块维护协议结构、封包和流式解析。协议层不依赖 HAL 或 FreeRTOS，也不硬编码具体 `cmd_id`。

帧格式：

```text
sof(1) + data_length(2) + seq(1) + crc8(1) + cmd_id(2)
+ payload(data_length) + crc16(2)
```

- SOF 固定为 `0xA5`。
- `data_length` 只表示 payload 长度。
- CRC8 校验 CRC8 字段之前的 4 字节，即 `sof + data_length + seq`。
- CRC16 校验除末尾 CRC16 外的完整数据包。
- `data_length`、`cmd_id` 和 CRC16 都按低字节在前传输。
- 浮点字段直接使用 32 位 IEEE 754 二进制布局；角度和速度单位由上下位机共同约定，协议层不做换算。
- 状态和控制 payload 均逐字段显式封包、解包，不通过整结构体复制隐含线上字段顺序。状态帧发送时直接给 `packet->header`、`packet->payload` 和 `packet->crc16` 成员赋值，不经过字段写入辅助函数或偏移量计算。

下位机状态 payload 为 22 字节，完整状态帧为 31 字节。视觉控制 payload 为 9 字节，完整控制帧为 18 字节。编译期 `_Static_assert` 会检查这五个固定尺寸，结构体字段变化但未同步协议时会直接构建失败。

修改现有消息时，需要同时修改 payload 结构体及其对应的封包或解包函数。新增消息类型时，建立独立 payload 结构体和独立封包、解包函数，不把无关数据合并进一个通用大结构体。

解析器支持 USB 分段和连包，不假设一次 CDC OUT 回调正好是一帧。遇到错误 CRC8、错误长度或错误 CRC16 时，从下一个可能的 `0xA5` 继续同步。

## 7. 底盘模块层

底盘模块位于 `User_File/3_Chariot/1_Module/Chassis`。

文件：

- `chassis_config.h`
- `chassis_config.c`
- `chassis_vmc.h`
- `chassis_vmc.c`
- `chassis_control.h`
- `chassis_control.c`
- `chassis_observer.h`
- `chassis_observer.c`
- `chassis_remote.h`
- `chassis_remote.c`

职责：

- 集中保存机械参数、传感器映射、电机极性、PID、LQR 和输出开关。
- 根据四个髋关节反馈计算左右五连杆状态，并完成 VMC 力矩映射。
- 直接维护十维整车状态、双腿长 K 矩阵、四路广义输出和最终电机命令。
- 根据 IMU、电机和 CAN 反馈生成故障标志。
- 默认保持安全零命令，不直接打开实机闭环输出。

这层使用唯一全局实际状态 `Chassis_t Chassis`。它同时是控制器运行状态和 Watch 入口，不再维护额外的 controller/debug 副本。

### 7.0 控制器子模块

当前控制器按五个直接职责拆开：

| 文件 | 职责 |
|---|---|
| `chassis_config.*` | 保存当前五连杆、轮、IMU 轴向、PID、LQR 拟合系数和输出开关 |
| `chassis_vmc.*` | `VMC_State_Calc()` 计算腿长/腿角及速度，`VMC_Torque_Calc()` 将虚拟力映射为两个髋关节力矩 |
| `chassis_control.*` | 维护 `Chassis`，在 `Chassis_Control()` 中完成速度融合、十维状态、支撑力、K、LQR、VMC 和输出限幅 |
| `chassis_observer.*` | 直接消费 `Chassis.body/leg/imu/dm_motor`，生成打滑、离地和转向只读观测量 |
| `chassis_remote.*` | 将通用遥控输入转换为 `Chassis.goal`、模式和动态输出许可 |

本次行为保持型精简后，`chassis_control.c` 从约1862行降到1714行，Chassis目录下`.c`总量从约3340行降到3190行。主要删除项为观测器输入快照、未使用广义力字段、完整上一周期腿状态副本、单行设置/查询包装和左右重复计算；TOP、STEP、恢复、板凳、观测器及安全零输出链均保留。

机械结构参数只支持当前这一套五连杆轮腿。后续如果仿真参数或实机尺寸变化，应优先修改 `chassis_config.c`，不要在 VMC、任务层或电机设备层散落硬编码参数。

### 7.0.1 SPR 参考工程对应关系

轮腿相关代码的流程参考已经验证过的 SPR 工程，但当前工程不会直接照搬 SPR 的状态机、参数、debug 结构或未接入功能。

SPR 参考工程路径：

```text
E:\AWL_WORK\Wheel-legged\Others\SPR Balanced-infantry\SPR Balanced-infantry\code\chassis\chassis
```

重点参考文件和函数：

| SPR 位置 | 参考内容 |
|---|---|
| `Task/chassis_task.c::chassis_feedback_update()` | 从电机、IMU 反馈计算虚拟腿几何状态、机体姿态、速度和控制状态 |
| `Task/chassis_task.c::chassis_control_loop()` | 控制环主顺序：支撑力、LQR K、运动输出、状态保护和控制模式分发 |
| `Task/chassis_task.c::chassis_stay_STANDING()` | 站立控制：LQR 轮/腿广义输出、轮力矩、电流转换、腿部 VMC 和左右腿协调 |

当前工程与 SPR 的主流程映射：

| SPR 流程 | 当前工程对应位置 | 当前处理方式 |
|---|---|---|
| `chassis_feedback_update()` | `chassis_task.c::Chassis_Feedback_Update()` | 直接把 IMU、DM、DJI 和 CAN 反馈写入 `Chassis` |
| `chassis_control_loop()` | `chassis_control.c::Chassis_Control()` | 在一个主函数中按阶段计算支撑力、K、四路 LQR、VMC 和电机命令 |
| `chassis_get_F()` | `Chassis_Control()` 第 3 阶段 | 公共 PID 计算腿长支撑力修正和横滚差动 |
| 生成本轮 K | `Chassis_Control()` 第 4 阶段 | 固定腿长和实测腿长共用 `Algorithm_LQR_FitLqrKPoly22()` |
| `chassis_stay_STANDING()` | `Chassis_Control()` 第 5 阶段 | 显式写出四组十维 `K * (target-state)` |
| `leg_force()` | `chassis_vmc.c::VMC_Torque_Calc()` | 输出左右腿前后髋关节力矩 |
| 电机命令发送 | `chassis_task.c::Chassis_Command_Send()` | 将 `Chassis.output.T_joint[]` 和 `Chassis.output.I_wheel[]` 下发 |

当前工程刻意没有接入 SPR 中的以下内容：

- 遥控器/云台模式状态机。
- 自起身、跌倒恢复和强制姿态切换。
- 跳跃、落地检测、KNN 台阶检测和冲击检测。
- 功率限制和超级电容相关控制。
- SPR 中按具体机器人调好的控制参数。

这样做的原因是当前工程先保留一条清楚、可 review、可安全构建的站立控制主链路。后续要加入上述能力时，应先明确输入输出和安全边界，再放入对应任务层或底盘模块，不要把 SPR 的全局状态和临时逻辑整体搬入本工程。

### 7.1 全局实际状态

`Chassis_t Chassis` 只保存一份实际状态，主要内容为：

- `enable_flag`、`mode`、`state`
- `imu`、4 个 `dm_motor`、2 个 `wheel_motor` 和 CAN 错误计数
- `body`、`leg[2]`、`goal`、`lqr`、`output` 和 `observer`
- `leg[].F0/Tp` 与 `output.T_wheel[]` 四路广义输出
- 请求输出 `output.T_joint_req/I_wheel_req` 和最终输出 `output.T_joint/I_wheel`
- `output.safe_flag`、`fault`、`calc_flag`
- PID 和速度 Kalman 的必要持久状态

任务层负责把设备反馈写入 `Chassis`，各控制阶段直接消费唯一状态。观测器不再使用每周期输入快照，也没有额外 debug 副本。

### 7.2 控制流程

当前 `Chassis_Control()` 的行为：

```text
输入状态
├── 检查外部使能
├── 检查 IMU 初始化和姿态可用
├── 检查 DM 电机在线
├── 检查 DJI 电机在线
├── 检查 CAN 发送错误计数
├── 计算左右五连杆状态和速度融合
├── 集中赋值十维状态并计算支撑力
├── 计算双腿长 K 和四路 LQR 输出
├── VMC 映射并完成力矩/电流限幅
├── 控制器异常时置 CHASSIS_FAULT_CONTROL
└── 根据编译宏和模型输出开关决定是否解除安全输出
```

当前即使没有故障，也仍然保持零输出。这是有意设计，因为 IMU 轴向、电机方向、轮电机电流换算和 LQR 参数尚未完成实机确认。

### 7.3 故障标志

`chassis_control.h` 定义了以下故障位：

| 故障位 | 含义 |
|---|---|
| `CHASSIS_FAULT_NONE` | 无故障 |
| `CHASSIS_FAULT_DISABLED` | 底盘未使能 |
| `CHASSIS_FAULT_IMU` | IMU 未就绪或有错误 |
| `CHASSIS_FAULT_DM_MOTOR` | 任一 DM 电机离线 |
| `CHASSIS_FAULT_DJI_MOTOR` | 任一 DJI 轮电机离线 |
| `CHASSIS_FAULT_CAN` | CAN 发送错误累计超过阈值 |
| `CHASSIS_FAULT_CONTROL` | 五连杆几何、配置或控制计算失败 |

### 7.4 第一阶段控制输出限制

控制器非零输出需要同时满足：

- `APP_CHASSIS_OUTPUT_ENABLE != 0`
- `Chassis_Config.output.joint_flag != 0` 或 `Chassis_Config.output.wheel_flag != 0`
- 对应力矩/电流限幅参数大于 0
- 底盘故障为 `CHASSIS_FAULT_NONE`

当前默认配置中上述开关均保持关闭，所以控制器只计算中间状态，最终下发仍为 DM 零力矩和 DJI 零电流。

### 7.5 当前控制状态量顺序

`chassis_config.h` 中定义的控制状态顺序与当前十维整车模型一致：

```text
0: s
1: dot_s
2: fai
3: dot_fai
4: theta_l
5: dot_theta_l
6: theta_r
7: dot_theta_r
8: theta_b
9: dot_theta_b
```

这也是 `Chassis.lqr.x[]` 的下标顺序和 `Chassis.lqr.K[row][col]` 的列顺序。`dot_s` 由公共 `Kalman` 融合得到，原始速度来自轮速、腿长、腿角速度，前向加速度来自 IMU 去重力后的运动加速度；`s` 由融合速度按配置限幅积分得到。

### 7.6 轮腿控制符号约定

本节记录当前工程参考 SPR 采用的状态定义，后续离线求 LQR 增益、实机电机方向映射和调试日志都应遵守这里的定义。

早期仿真控制器不再作为当前单片机工程的符号依据。电机力矩正负方向需要通过 `torque_scale` 和实机低力矩测试最终确认。

腿角定义：

```text
theta_l = phi0_l - phi0_offset - theta_b
theta_r = phi0_r - phi0_offset - theta_b
```

其中 `phi0_offset` 当前为 `pi/2`，`phi0_l/phi0_r` 是五连杆几何计算得到的虚拟腿角，`theta_b` 是机体俯仰角。`phi0`保留`atan2`主值，`phi0_total`用于连续多圈目标；送入LQR的`theta_l/theta_r`选择目标平衡点附近的等价角。该定义参考 SPR 的 `left_theta = leg_phi0 - 1.57 - angle_pitch`。

LQR的`fai`使用IMU已有的连续`yawTotalRad`，进入站立或板凳时以当前连续yaw作为目标。USB显示仍使用单圈`yawRad`。

当前软件方向：

```text
theta_l > 0：左虚拟腿方向按 SPR 状态定义为正
theta_r > 0：右虚拟腿方向按 SPR 状态定义为正
dot_theta_l/dot_theta_r > 0：对应 SPR 腿角速度正方向
```

控制输入顺序：

```text
T_l  = 左轮轮端力矩，单位 N*m
T_r  = 右轮轮端力矩，单位 N*m
T_pl = 左虚拟腿摆动力矩，单位 N*m
T_pr = 右虚拟腿摆动力矩，单位 N*m
```

当前软件力矩方向：

```text
T_pl > 0：按 SPR 状态定义作用在左虚拟腿摆动通道
T_pr > 0：按 SPR 状态定义作用在右虚拟腿摆动通道
```

注意：这里定义的是控制层广义力矩方向，不等同于 DM 电机原始力矩方向。具体电机安装导致的正负号应在 `chassis_config.c` 的 `angle_scale` 和 `torque_scale` 中处理，不允许在 LQR 增益、任务层或设备层里临时补反号。

### 7.7 LQR 增益拟合与腿长来源

当前工程已经接入 LQR K 拟合链路，但仍保持安全零输出。控制器会先根据配置得到本轮实际使用的 K 矩阵，再用同一套状态反馈公式计算广义输出。

通用拟合算法位于：

```text
User_File/1_Middleware/2_Algorithm/LQR.h
User_File/1_Middleware/2_Algorithm/LQR.c
```

该算法只做一件事：根据左右腿长计算 `poly22` 二元二次拟合结果。它不依赖底盘模型、电机状态、FreeRTOS 或 HAL，因此后续其他模块如果需要同类 LQR K 拟合，也应复用这一份代码。

拟合公式为：

```text
K(row, col) = p00 + p10*x + p01*y + p20*x^2 + p11*x*y + p02*y^2
```

其中：

| 变量 | 含义 |
|---|---|
| `x` | 左腿长，单位 m |
| `y` | 右腿长，单位 m |
| `row` | 控制输出行，顺序为 `T_l, T_r, T_pl, T_pr` |
| `col` | 状态列，顺序为 `s, dot_s, fai, dot_fai, theta_l, dot_theta_l, theta_r, dot_theta_r, theta_b, dot_theta_b` |

系数顺序必须与 MATLAB `fit(..., "poly22")` 的 `coeffvalues()` 一致：

```text
p00, p10, p01, p20, p11, p02
```

底盘默认配置位于：

```text
User_File/3_Chariot/1_Module/Chassis/chassis_config.c
```

关键配置字段：

| 字段 | 当前值 | 含义 |
|---|---:|---|
| `lqr.enabled` | `1U` | 只启用 K 拟合计算，不代表允许电机非零输出 |
| `lqr.length_source` | `CHASSIS_K_LENGTH_FIXED` | 默认使用固定腿长，便于先做定腿长方向调试 |
| `lqr.min_leg_length_m` | `0.15f` | K 拟合最小有效腿长 |
| `lqr.max_leg_length_m` | `0.35f` | K 拟合最大有效腿长 |
| `lqr.fixed_left_length_m` | `0.25f` | 定腿长模式左腿输入 |
| `lqr.fixed_right_length_m` | `0.25f` | 定腿长模式右腿输入 |
| `lqr.coefficients` | 当前 MATLAB 输出 | `lqr.K(row,col)` 的 40 组 `poly22` 系数 |

定腿长和变腿长共用同一套代码，区别只在腿长来源：

```text
CHASSIS_K_LENGTH_FIXED
└── 使用 fixed_left_length_m / fixed_right_length_m

CHASSIS_K_LENGTH_MEASURED
└── 使用 Chassis.leg[].L0
```

两种模式之后都会进入同一个流程：

```text
左右腿长
└── Algorithm_LQR_FitLqrKPoly22()
    └── Chassis.lqr.K[4][10]
        └── output.T_wheel / leg[].Tp = K * (lqr.target - lqr.x)
```

直接在 Watch 中展开全局变量 `Chassis`：

| 字段 | 含义 |
|---|---|
| `lqr.K` | 本轮实际用于状态反馈的 K 矩阵 |
| `leg[].K_L0` | 送入拟合前的左右腿长 |
| `leg[].K_L0_fit` | 限制到拟合范围后的左右腿长 |
| `lqr.fit_flag` | 本轮是否使用拟合 K |
| `lqr.limit_flag` | 腿长是否被限制到 `0.15~0.35m` |

当前 `lqr.coefficients` 使用的是已有 MATLAB 输出。已检查 `ABK_LQR.m` 当前脚本仍以 `0.12~0.31m` 采样，而固件输入限幅已经设为 `0.15~0.35m`。因此 `0.31~0.35m` 仍属于外推区；要严格覆盖目标范围，需要先修改 MATLAB 采样区间并重新生成系数，再替换 `chassis_config.c` 中的表。

当前反馈链路不使用 `A_Fit_Coefficients` 和 `B_Fit_Coefficients`。原因是本阶段控制器只需要离线求好的反馈增益 K；A/B 拟合系数主要用于后续在线模型预测、MPC、观测器或在线重新计算控制律。如果后续要做这些功能，应在公共算法层新增明确的模型预测或矩阵计算模块，而不是把 A/B 系数散落到底盘控制器主流程里。

## 8. 任务层

任务层位于 `User_File/5_Task`。

任务层负责 FreeRTOS 调度、模块连接和数据分发。任务层可以调用设备层和模块层，但不应该实现复杂控制算法。

### 8.1 CAN 任务：`task_can`

文件：

- `task_can.h`
- `task_can.c`

职责：

- 初始化 FDCAN1 和 FDCAN2 驱动。
- 提供 CAN 发送缓存表。
- 提供 CAN 接收消息队列。
- 将驱动层收到的报文转成 `task_can_rx_message_t`。
- 调用 `CAN_Task_RxMessageCallback()` 做应用层分发。
- 周期性发送所有已启用缓存帧。

主要内部对象：

| 对象 | 作用 |
|---|---|
| `canTxFrames[]` | 发送缓存槽 |
| `canTxMutex` | 保护发送缓存 |
| `canRxQueue` | 接收消息队列 |
| `canTxBusyCount` | 发送 FIFO 忙计数 |
| `canTxErrorCount` | 发送错误计数 |
| `canRxOverflowCount` | 接收队列溢出计数 |

CAN 接收流程：

```text
FDCAN 中断
└── driver_fdcan
    └── CAN_Task_RxCallback()
        ├── 写入 canRxQueue
        └── 设置 CAN_TASK_FLAG_RX

CanTask()
└── CAN_Task_DispatchRx()
    └── CAN_Task_RxMessageCallback()
        ├── FDCAN1 -> Motor_DM_UpdateFeedback()
        └── FDCAN2 -> Motor_DJI_UpdateFeedback()
```

CAN 发送流程：

```text
上层模块
├── CAN_Task_UpdateTxFrame()
│   └── 写入或更新 canTxFrames[]
└── CAN_Task_RequestTx()
    └── 设置 CAN_TASK_FLAG_TX

CanTask()
└── CAN_Task_SendFrames()
    └── Driver_FDCAN_SendData()
```

DJI 轮电机电流发送：

```text
CAN_Task_SetDjiCurrent()
└── 打包 ID 0x200 的 8 字节控制帧
    └── CAN_Task_UpdateTxFrame(&hfdcan2, 0x200, ...)
```

### 8.2 IMU 任务：`task_imu`

文件：

- `task_imu.h`
- `task_imu.c`

职责：

- 初始化 BMI088。
- 固定周期读取 BMI088。
- 启动阶段估计陀螺零偏。
- 用互补滤波维护基础 roll、pitch、yaw。
- 保存线程安全的 IMU 状态快照。

IMU 任务状态 `task_imu_state_t` 包含：

- BMI088 是否初始化
- 姿态是否准备好
- 初始化错误计数
- 读取错误计数
- 最近错误码
- BMI088 数据快照
- 陀螺零偏
- roll、pitch、yaw
- 去重力后的自然坐标系运动加速度 `motionAccMps2`
- 最近姿态积分周期

当前姿态估计说明：

- roll 和 pitch 使用陀螺积分，并用加速度低频修正。
- yaw 只做陀螺积分。
- 当前没有磁力计或外部航向观测，因此 yaw 不能视为长期绝对航向。
- `motionAccMps2` 先按当前姿态扣除机体系重力，再转到自然坐标系；底盘前向轴由 `chassis_config.c` 中的 `forward_accel_axis` 和 `forward_accel_scale` 映射。
- 完成 `APP_CONFIG_IMU_BIAS_SAMPLE_COUNT` 次零偏采样后，`isAttitudeReady` 才会置 1。

### 8.3 底盘任务：`chassis_task`

文件：

- `chassis_task.h`
- `chassis_task.c`

职责：

- 初始化 DM 电机设备层。
- 初始化全局 `Chassis`、PID 和速度 Kalman。
- 周期性收集 IMU、电机、CAN 状态。
- 用 `switch-case` 按当前状态进入站立控制或安全零输出。
- 将 `Chassis.output` 中的最终命令下发给 DM 和 DJI。
- 对外只保留任务初始化和遥控输入接口；调试直接查看全局 `Chassis`。

底盘任务主流程：

```text
Chassis_Task_Entry()
├── Motor_DM_Init()
├── Motor_DM_SetSafe(1)
├── Motor_DM_ZeroAll()
├── Motor_DM_SetEnable(1)
└── while (1)
    ├── Chassis_Feedback_Update()
    │   ├── IMU_Task_GetState()
    │   ├── Motor_DM_GetState()
    │   ├── Motor_DM_IsOnline()
    │   ├── Motor_DJI_IsOnline()
    │   └── CAN_Task_GetTxErrorCount()
    ├── Chassis_Leg_Update()
    ├── Chassis_State_Update()
    ├── switch (Chassis.state)
    │   ├── CHASSIS_STANDING -> Chassis_Control()
    │   ├── CHASSIS_FALLEN/FALLING_TO_STAND -> Chassis_Recovery()
    │   ├── CHASSIS_BENCH -> Chassis_Bench()
    │   ├── CHASSIS_STEP -> Chassis_Step()
    │   └── CHASSIS_ZERO_FORCE -> Chassis_Zero_Output()
    ├── Chassis_Command_Send()
    │   ├── Motor_DM_SetSafe()
    │   ├── Motor_DM_ZeroAll()
    │   ├── Motor_DM_SetCommand()
    │   ├── CAN_Task_SetDjiCurrent()
    │   ├── Motor_DM_UpdateTxFrames()
    │   └── CAN_Task_RequestTx()
    └── osDelayUntil()
```

外部可用接口：

| 接口 | 作用 |
|---|---|
| `Chassis_Task_Init()` | 初始化控制状态并创建底盘任务 |
| `Chassis_Remote_Update()` | 把通用遥控输入转换为模式和物理目标 |

当前底盘外部模式：

| 模式 | 当前行为 |
|---|---|
| `CHASSIS_MODE_ZERO_FORCE` | 主动零力，持续覆盖安全零输出 |
| `CHASSIS_MODE_FOLLOW` | 进入 `CHASSIS_STANDING`，调用 `Chassis_Control()` |
| `CHASSIS_MODE_TOP` | 右摇杆生成yaw速度，左摇杆XY按连续yaw相位投影到轮式前进方向；LQR屏蔽位移和yaw位置反馈 |
| `CHASSIS_MODE_SELF_SAVE` | 单次触发倒地转腿、收成小板凳并满足稳定条件后进入站立 |
| `CHASSIS_MODE_BENCH` | 四关节保持小板凳目标，双轮使用当前十维LQR的左右轮输出 |
| `CHASSIS_MODE_STEP` | `PREPARE -> APPROACH -> CLIMB -> RECOVER`四阶段辅助爬台阶，接触后轮实际输出清零但保留请求量 |

`Chassis.enable_flag=0`只禁止最终非零命令，不阻断当前模式的VMC、PID、LQR和请求量计算，并把故障位标记为`CHASSIS_FAULT_DISABLED`。DM协议保持使能以持续反馈。该标志为1后仍需同时满足总开关、分路开关、设备在线和限幅条件才会产生非零命令。

调试时把全局 `Chassis` 加入 Watch。恢复和板凳模式重点看 `state_time`、`stable_time`、`leg[].target_L0/target_phi0`、`output.target_angle/target_speed` 和 `output.T_joint_req`。板凳轮控制另外观察 `lqr.x`、`output.T_wheel` 和 `output.I_wheel_req`；最终命令看 `output.T_joint/I_wheel`。`calc_flag=1`只表示本轮算法链完整，允许与非零`fault`同时出现。

### 8.4 应用分发：`task_can_dispatch`

文件：

- `task_can_dispatch.h`
- `task_can_dispatch.c`

职责：

- 定义 DJI 轮电机反馈数组 `chassisDjiWheels[]`。
- 实现 `CAN_Task_RxMessageCallback()`。
- 根据 CAN 总线和 ID 把接收帧分发到对应设备层。

当前分发规则：

| 总线 | 条件 | 分发目标 |
|---|---|---|
| FDCAN1 | 长度为 DM 帧长度 | `Motor_DM_UpdateFeedback()` |
| FDCAN2 | ID `0x201` | `Motor_DJI_UpdateFeedback(&chassisDjiWheels[0])` |
| FDCAN2 | ID `0x202` | `Motor_DJI_UpdateFeedback(&chassisDjiWheels[1])` |

### 8.5 USB 任务：`task_usb`

USB 任务负责把中断字节流、通信协议和业务回调连接起来：

```text
USB OTG HS 中断
└── CDC_Receive_HS()
    └── Driver_USB_Receive()
        └── 2 KB 接收环形缓冲
            └── UsbTask()
                └── USB_Protocol_Parse()
                    └── USB_Task_FrameReceived()
```

发送流程：

```text
UsbTask 每 2 ms 通过 IMU_Task_GetState() 读取姿态角
└── USB_Task_SendStatus(usbTaskTxCommandId, usbTaskTxData)
    └── 31 字节状态帧 FIFO
        └── UsbTask() 最多合并 16 帧
            └── 496 字节 CDC IN 缓冲
                └── Driver_USB_Send()
```

`task_usb.c` 中的 `usbTaskTxData` 是实际发送数据，不是额外副本。`roll`、`yaw`、`pitch` 每个 USB 状态周期通过 IMU 公开接口读取右手系状态；`bulletSpeed`、`heat`、`enemyColor`、`mode` 暂无数据源，初始化为 0，可直接修改变量初始化值或在 Live Watch 中修改。`usbTaskTxCommandId` 是实际发送命令号，调试默认值为 `0x0002`，上下位机确定命令号后直接修改该变量。USB 状态发送不再依赖底盘任务。

发送 FIFO 固定保存 32 帧。队列满时覆盖最旧帧；USB 未连接或提交失败时丢弃当前批次，不阻塞控制任务，也不反复发送过期状态。`USBD_BUSY` 表示上一批仍在发送，此时保留当前批次等待下一次完成回调。

接收有效控制帧后，`USB_Protocol_UnpackControl()` 逐项解析 `yaw`、`pitch` 和 `shoot`，任务再更新 `usbTaskDebugState.recentControl` 并调用通过 `USB_Task_SetControlHandler()` 注册的业务回调。由于当前尚未确定具体命令号，USB 层只透传 `commandId`，不做命令号过滤。

Watch 窗口可直接观察 `usbTaskDebugState`：

- `rxParser`：流式解析器当前状态；其 `headerCrcErrorCount`、`frameCrcErrorCount`、`lengthErrorCount` 分别累计帧头 CRC8、整帧 CRC16 和 payload 长度错误。
- `recentControl`：最近一帧有效控制数据，包含 `yaw`、`pitch` 和 `shoot`。
- `recentControlCmdId`、`recentControlSeq`：最近有效控制帧的命令号和序号。
- `recentRxSeq`：最近一帧 CRC8、CRC16 均正确的协议序号。
- `rxByteCount`：本次上电后 CDC OUT 收到的累计字节数。
- `rxFrameCount`：本次上电后 CRC8、CRC16 均正确的累计帧数，包含重复帧。
- `rxLostCount`：本次上电后根据序号跳跃推算出的累计缺失帧数。
- `rxDuplicateCount`：本次上电后与上一有效帧序号相同的累计重复帧数。
- `rxSeqResetCount`：序号出现大跨度反向变化时重新同步的累计次数。USB 不会重排字节，因此该值增加通常表示上位机重启或序号源被重置。
- `rxControlCount`：payload 长度符合控制数据结构的累计帧数。
- `rxOtherCount`：CRC 正确但 payload 长度不符合控制数据结构的累计帧数。
- `rxOverflowCount`：接收环形缓冲满时未能写入的累计字节数，正常运行应保持 0。
- `txQueuedCount`：本次上电后写入发送 FIFO 的累计状态帧数。
- `txCompleteCount`：本次上电后收到 CDC IN 完成回调的累计状态帧数。
- `txDroppedCount`：本次上电后因 FIFO 覆盖、USB 断开或发送错误而丢弃的累计帧数。
- `txBusyCount`：CDC 返回 `USBD_BUSY` 的累计次数。当前批次会保留并在后续继续提交，因此它本身不等于丢帧。
- `txErrorCount`：CDC 返回非 `USBD_OK`、非 `USBD_BUSY` 错误的累计次数。
- `rxByteRateBps`、`rxFrameRateHz`：最近一秒的接收吞吐量和 CRC 正确帧频。
- `rxLossPercent`：本次上电以来的累计接收丢包率，计算式为 `rxLostCount / (rxFrameCount - rxDuplicateCount + rxLostCount) * 100%`。支持 `255 -> 0` 序号回绕，重复帧不计入期望帧总数。
- `txByteRateBps`、`txFrameRateHz`：最近一秒实际完成的发送吞吐量和帧频，状态周期为 2 ms 时帧频应接近 500 Hz。
- `txLossPercent`：本次上电以来的下位机本地累计发送丢弃率，计算式为 `txDroppedCount / (txCompleteCount + txDroppedCount) * 100%`。它只能说明下位机队列和 USB 提交情况，不能证明上位机已经正确解析数据。

## 9. 启动流程

系统启动大致流程如下：

```text
main()
├── HAL_Init()
├── SystemClock_Config()
├── MX_GPIO_Init()
├── MX_FDCAN1_Init()
├── MX_FDCAN2_Init()
├── MX_SPI2_Init()
├── osKernelInitialize()
├── MX_FREERTOS_Init()
│   ├── defaultTask
│   ├── CAN_Task_Init()
│   ├── Chassis_Task_Init()
│   ├── IMU_Task_Init()
│   └── USB_Task_Init()
│       ├── 注册 CDC 接收、发送完成和断开回调
│       └── 创建 UsbTask
└── osKernelStart()
```

任务创建后：

```text
CanTask
├── Driver_FDCAN_Init(FDCAN1)
└── Driver_FDCAN_Init(FDCAN2)

chassis_task
├── Motor_DM_Init()
├── Motor_DM_SetSafe(1)
├── Motor_DM_ZeroAll()
└── Motor_DM_SetEnable(1)

ImuTask
└── BMI088_Init()

UsbTask
├── 处理 CDC OUT 环形缓冲
└── 批量提交 CDC IN 状态帧

defaultTask
└── MX_USB_DEVICE_Init()
```

USB 回调必须在 USB Device 外设启动前注册，因此 `USB_Task_Init()` 在启动调度器前完成回调注册；真正的 `MX_USB_DEVICE_Init()` 保留在 CubeMX 生成的 `defaultTask` 中，并且只能调用一次。

## 10. 运行期数据流

### 10.1 传感器数据流

```text
BMI088 硬件
└── SPI2
    └── driver_spi
        └── device_bmi088
            └── task_imu
                └── task_imu_state_t
                    └── chassis_task
                        └── chassis.imu
```

### 10.2 电机反馈数据流

```text
DM/DJI 电机反馈
└── FDCAN
    └── driver_fdcan
        └── task_can
            └── CAN_Task_RxMessageCallback()
                ├── device_motor_dm
                └── device_motor_dji
```

### 10.3 控制输出数据流

```text
chassis_task
└── Chassis_Control()
    ├── Chassis.output.T_joint_req / I_wheel_req
    └── Chassis.output.T_joint / I_wheel
        ├── Motor_DM_SetCommand()
        ├── Motor_DM_UpdateTxFrames()
        ├── CAN_Task_SetDjiCurrent()
        └── CAN_Task_RequestTx()
            └── task_can
                └── driver_fdcan
                    └── FDCAN 硬件
```

### 10.4 USB 上下位机数据流

```text
下位机状态 -> USB_Task_SendStatus() -> CRC8/CRC16 封包 -> 状态帧 FIFO
             -> CDC IN -> 上位机

上位机 -> CDC OUT -> 接收环形缓冲 -> 流式解析和 CRC 校验
       -> usbTaskDebugState.recentControl -> 已注册业务回调
```

## 11. 当前安全策略

当前框架默认面向实机安全。

核心规则：

- 上电默认 `chassis.enabled = 0`，表示非零输出许可关闭。
- DM 电机默认 `dmSafe = 1`。
- 底盘模块默认 `Chassis.output.safe_flag = 1`。
- 底盘任务每个周期都下发零输出。
- DM协议自动使能以持续取得关节反馈，但不会解除安全零力矩。
- 设备未就绪或故障时继续计算控制中间量，并持续覆盖最终零输出。
- 控制器模块保留请求量供Watch观察，但默认没有任何路径允许非零底盘控制命令下发。

故障来源包括：

- 底盘未使能。
- IMU 未初始化。
- IMU 姿态未准备好。
- IMU 最近错误码非零。
- 任一 DM 电机离线。
- 任一 DJI 轮电机离线。
- CAN 发送错误计数超过阈值。
- 五连杆几何或控制器计算失败。

为什么故障时仍然持续发送零输出：

- 如果停止发送，电机或发送缓存可能保留上一帧命令。
- 持续覆盖零命令可以降低旧命令残留风险。
- 这是重新搭框架阶段更保守的行为。

## 12. 当前没有实现或未启用的内容

以下能力当前没有实现：

- 轮速闭环。
- 跳跃。
- 落地检测。
- 打滑检测。
- 遥控器或上位机命令解析。
- 功率限制。
- 完整急停输入链路。
- 已完成实机验证并允许非零输出的 LQR 闭环。

以下能力已经有第一阶段计算链，但默认未启用实机非零输出：

- 五连杆正运动学和速度计算。
- VMC 虚拟支撑力/腿摆力矩到髋关节力矩映射。
- 基于公共 `LQR` 的左右腿长 `poly22` 拟合 K 计算。
- 基于公共 `PID` 的腿长 PID 虚拟支撑力。
- 基于公共 `PID` 的横滚 PID 虚拟支撑力差。
- 基于公共 `Kalman` 的 `dot_s` 速度融合和 `s` 位移积分。
- 控制状态向量组装。

这些功能后续应接入到明确的层级里，不要直接塞进驱动层或设备层。

## 13. 后续扩展建议

### 13.1 启用 LQR 和非零输出

建议入口：

```text
User_File/3_Chariot/1_Module/Chassis/chassis_control.c
User_File/3_Chariot/1_Module/Chassis/chassis_config.c
```

推荐步骤：

1. 台架确认 IMU 的 roll、pitch、yaw 和 gyro 正负方向。
2. 台架确认 4 个 DM 髋关节电机的零位、方向和力矩方向。
3. 根据当前机械参数和 `0.15~0.35m` 目标范围离线生成 LQR K 拟合系数，并替换 `chassis_config.c` 中的 `lqr.coefficients`。
4. 确认 DJI 轮电机型号、减速比和轮端力矩到原始电流的换算关系。
5. 定腿长调试阶段保持 `CHASSIS_K_LENGTH_FIXED`，确认符号后再切换为 `CHASSIS_K_LENGTH_MEASURED`。
6. 在 `chassis_config.c` 中填写限幅和输出开关。
7. 在 `app_config.h` 中打开 `APP_CHASSIS_OUTPUT_ENABLE`。
8. `Chassis_Control()` 只在以下条件同时满足时生成非零输出：
   - `chassis.enabled != 0`
   - `chassis.fault == CHASSIS_FAULT_NONE`
   - IMU、DM、DJI和CAN通信正常
   - 控制参数已经标定
   - 输出经过限幅
9. 保留 `safe_flag` 路径。
10. 不要在驱动层、设备层或任务层直接写控制算法。

当前建议控制链路：

```text
设备状态
└── chassis_task 写入 chassis
    └── Chassis_Control()
        ├── chassis_vmc
        ├── PID / Kalman / LQR
        └── 安全输出和限幅
```

### 13.2 新增设备

新增设备时按以下顺序做：

1. 在 `2_Device` 下建立设备模块。
2. 设备模块只负责协议解析和状态维护。
3. 如需硬件收发，优先复用 `driver_fdcan` 或 `driver_spi`。
4. 在任务层添加调度或分发。
5. 必要时在 `app_config.h` 中集中添加 ID、数量和超时。
6. 更新 `CMakeLists.txt`。

不要让设备层创建 FreeRTOS 任务，除非这个设备确实需要独立调度，并且已经明确设计边界。

### 13.3 新增遥控器或上位机

当前不再单独建立交互层。USB CDC 已提供通用收发链路；新增遥控器、上位机命令或裁判系统数据时，按职责放置：

- 纯协议解析、设备状态维护放在 `2_Device`。
- FreeRTOS 调度、消息分发、命令汇总放在 `5_Task`。
- 底盘控制目标和故障输入最终写入唯一全局状态 `Chassis`。
- 新的 USB 业务命令在任务层根据 `cmd_id` 分发，不把命令号判断写入 `driver_usb` 或 CRC 模块。
- 新增 USB 消息时建立独立 payload 结构体，并在协议层逐字段实现对应的封包或解包函数。

新增这类输入时不要直接写电机电流，也不要绕过 `Chassis_Control()`。任务层收集 IMU、电机、CAN 和外部命令后，写入 `Chassis` 对应字段。

### 13.4 新增故障处理

新增故障时优先在 `chassis_control.h` 中增加故障位，例如：

```c
#define CHASSIS_FAULT_REMOTE 0x00000040UL
```

然后在 `Chassis_Control()` 的安全检查阶段集中判断。

不要在多个任务中分别维护同一类故障事实。底盘故障最终汇总到 `chassis.fault`。

## 14. 新人推荐阅读顺序

第一次接手代码时，建议按这个顺序读：

1. `代码风格要求.md`
2. `代码相关介绍.md`
3. `CMakeLists.txt`
4. `Core/Src/freertos.c`
5. `User_File/1_Middleware/0_Common/app_config.h`
6. `User_File/1_Middleware/2_Algorithm/PID.c`
7. `User_File/1_Middleware/2_Algorithm/LQR.c`
8. `User_File/1_Middleware/2_Algorithm/Kalman.c`
9. `User_File/1_Middleware/2_Algorithm/CRC.c`
10. `User_File/1_Middleware/1_Driver/USB/driver_usb.c`
11. `User_File/2_Device/Communication/USB/device_usb_protocol.c`
12. `User_File/5_Task/task_usb.c`
13. `User_File/5_Task/task_can.c`
14. `User_File/5_Task/task_can_dispatch.c`
15. `User_File/2_Device/Motor/Motor_DM/device_motor_dm.c`
16. `User_File/2_Device/Motor/Motor_DJI/device_motor_dji.c`
17. `User_File/2_Device/IMU/BMI088/device_bmi088.c`
18. `User_File/5_Task/task_imu.c`
19. `User_File/5_Task/chassis_task.c`
20. `User_File/3_Chariot/1_Module/Chassis/chassis_config.c`
21. `User_File/3_Chariot/1_Module/Chassis/chassis_vmc.c`
22. `User_File/3_Chariot/1_Module/Chassis/chassis_control.c`
23. SPR 参考工程 `Task/chassis_task.c` 中的 `chassis_feedback_update()`、`chassis_control_loop()`、`chassis_stay_STANDING()`

如果你只想看主数据流，优先读：

```text
freertos.c
├── task_usb.c
    └── device_usb_protocol.c
├── task_can.c
├── task_imu.c
└── chassis_task.c
    └── chassis_control.c
        └── chassis_vmc.c
```

## 15. 修改代码前检查清单

每次改代码前先确认：

- 是否已经阅读 `代码风格要求.md`。
- 要改的是驱动、设备、模块还是任务层。
- 这个改动是否会让下层依赖上层业务。
- 是否新增了未使用函数、变量、宏或中间状态。
- 是否需要同步 `CMakeLists.txt`。
- 是否需要把硬件 ID、数量或超时放入 `app_config.h`。
- 是否破坏默认安全零输出。
- 是否需要新增或更新本文档。
- 如果修改轮腿控制主链路，是否已经对照 SPR 的 `chassis_control_loop()`，并确认没有顺手改动当前参数配置或 debug 结构。

## 16. 验证建议

基础验证命令：

```powershell
cmake --build build/Debug
```

公共角度算法主机侧测试：

```powershell
gcc -std=c11 -Wall -Wextra -Werror `
  -IUser_File/1_Middleware/2_Algorithm `
  Tests/test_angle.c `
  User_File/1_Middleware/2_Algorithm/Angle.c `
  -lm -o build/angle_test.exe
./build/angle_test.exe
```

DM反馈与纯力矩发送主机侧测试：

```powershell
gcc -std=c11 -Wall -Wextra -Werror `
  -Wno-error=int-to-pointer-cast -Wno-error=pointer-to-int-cast `
  -DUSE_HAL_DRIVER -DSTM32H723xx `
  -ICore/Inc -IDrivers/STM32H7xx_HAL_Driver/Inc `
  -IDrivers/STM32H7xx_HAL_Driver/Inc/Legacy `
  -IDrivers/CMSIS/Device/ST/STM32H7xx/Include -IDrivers/CMSIS/Include `
  -IUser_File/1_Middleware/0_Common -IUser_File/1_Middleware/2_Algorithm `
  -IUser_File/2_Device/Motor/Motor_DM -IUser_File/5_Task `
  Tests/test_motor_dm.c User_File/1_Middleware/2_Algorithm/Angle.c `
  User_File/2_Device/Motor/Motor_DM/device_motor_dm.c `
  -lm -o build/motor_dm_test.exe
./build/motor_dm_test.exe
```

USB 协议主机侧测试：

```powershell
gcc -std=c11 -Wall -Wextra -Werror `
  -IUser_File/1_Middleware/2_Algorithm `
  -IUser_File/2_Device/Communication/USB `
  Tests/test_usb_protocol.c `
  User_File/1_Middleware/2_Algorithm/CRC.c `
  User_File/2_Device/Communication/USB/device_usb_protocol.c `
  -o build/usb_protocol_test.exe
./build/usb_protocol_test.exe
```

五连杆 VMC 主机侧回归测试：

```powershell
gcc -std=c11 -Wall -Wextra -Werror `
  -IUser_File/1_Middleware/0_Common `
  -IUser_File/1_Middleware/2_Algorithm `
  -IUser_File/3_Chariot/1_Module/Chassis `
  Tests/test_chassis_vmc.c `
  User_File/1_Middleware/2_Algorithm/Angle.c `
  User_File/3_Chariot/1_Module/Chassis/chassis_vmc.c `
  -lm -o build/chassis_vmc_test.exe
./build/chassis_vmc_test.exe
```

底盘只读观测器主机侧测试：

```powershell
gcc -std=c11 -Wall -Wextra -Werror -DDISABLEFLOAT16 `
  -IUser_File/1_Middleware/0_Common `
  -IUser_File/1_Middleware/2_Algorithm `
  -IUser_File/3_Chariot/1_Module/Chassis `
  -IMiddlewares/ST/ARM/DSP/Include `
  -IMiddlewares/ST/ARM/DSP/PrivateInclude `
  -IDrivers/CMSIS/Include `
  Tests/test_chassis_observer.c `
  User_File/1_Middleware/2_Algorithm/Angle.c `
  User_File/3_Chariot/1_Module/Chassis/chassis_vmc.c `
  User_File/3_Chariot/1_Module/Chassis/chassis_observer.c `
  -lm -o build/chassis_observer_test.exe
./build/chassis_observer_test.exe
```

板凳与重新站立状态机主机侧测试：

```powershell
gcc -std=c11 -Wall -Wextra -Werror `
  -IUser_File/1_Middleware/0_Common `
  -IUser_File/1_Middleware/2_Algorithm `
  -IUser_File/3_Chariot/1_Module/Chassis `
  -IMiddlewares/ST/ARM/DSP/Include `
  -IMiddlewares/ST/ARM/DSP/PrivateInclude `
  -IDrivers/CMSIS/Include `
  -DDISABLEFLOAT16 `
  Tests/test_chassis_recovery.c `
  User_File/1_Middleware/2_Algorithm/Angle.c `
  User_File/1_Middleware/2_Algorithm/Kalman.c `
  User_File/1_Middleware/2_Algorithm/LQR.c `
  User_File/1_Middleware/2_Algorithm/PID.c `
  User_File/3_Chariot/1_Module/Chassis/chassis_config.c `
  User_File/3_Chariot/1_Module/Chassis/chassis_control.c `
  User_File/3_Chariot/1_Module/Chassis/chassis_vmc.c `
  User_File/3_Chariot/1_Module/Chassis/chassis_observer.c `
  Middlewares/ST/ARM/DSP/Source/MatrixFunctions/arm_mat_init_f32.c `
  Middlewares/ST/ARM/DSP/Source/MatrixFunctions/arm_mat_add_f32.c `
  Middlewares/ST/ARM/DSP/Source/MatrixFunctions/arm_mat_sub_f32.c `
  Middlewares/ST/ARM/DSP/Source/MatrixFunctions/arm_mat_mult_f32.c `
  Middlewares/ST/ARM/DSP/Source/MatrixFunctions/arm_mat_trans_f32.c `
  Middlewares/ST/ARM/DSP/Source/MatrixFunctions/arm_mat_inverse_f32.c `
  -lm -o build/chassis_recovery_test.exe
./build/chassis_recovery_test.exe
```

如果 `cmake --build` 在本机环境卡住，可直接调用 CMake 缓存中的 Ninja。当前工程缓存中的构建器路径可在 `build/Debug/CMakeCache.txt` 的 `CMAKE_MAKE_PROGRAM` 查看。

示例：

```powershell
& 'C:\Users\25697\AppData\Local\stm32cube\bundles\ninja\1.13.2+st.1\bin\ninja.exe' -C 'E:\AWL_WORK\Wheel-legged\Wheel-legged\build\Debug' -d explain Wheel-legged
```

静态检查建议：

```powershell
rg -n "drv_|dvc_|mod_|motor_task|daemon_task|task_config_and_callback" User_File CMakeLists.txt Core\Src\freertos.c
rg -n "HAL_StatusTypeDef .*\\(" User_File
rg -n "TODO|Mock|Stub|Demo" User_File
rg -n "module_chassis|Module_Chassis|chassisControllerDebug|ChassisTask_" User_File Core CMakeLists.txt
```

期望结果：

- 构建成功或 Ninja 显示 `no work to do`。
- 旧任务名不应再出现在有效用户代码和构建入口中。
- 模块公共接口不应直接返回 `HAL_StatusTypeDef`。
- 不应出现 Mock、Stub 或 Demo 级替代逻辑。

## 17. 常见问题

### 为什么 `Chassis_Control()` 现在总是零输出？

这是当前框架阶段的安全设计。控制器已经可以计算中间量，但 LQR 参数、IMU 轴向、电机方向和轮电机电流换算尚未完成实机验证，因此不能让实机产生未经验证的非零输出。

### 为什么设备层不直接发电机命令？

设备层只维护设备状态和协议格式。是否发命令、何时发命令、发什么命令由任务层和底盘模块决定。

### 为什么硬件 ID 放在 `app_config.h`？

硬件映射是全局事实，集中配置可以避免多个文件各自维护一份 ID。

### 为什么驱动层仍然使用 HAL？

驱动层本来就直接面对硬件库。当前风格是不把 HAL 返回值转换成跨模块统一状态码；公共接口默认 `void`，上层通过在线状态、错误码、故障位和安全零输出判断系统是否允许控制。

### 什么时候可以让电机非零输出？

至少需要满足：

- 控制算法有明确设计依据。
- 电机方向、单位、限幅已核对。
- `Chassis_Control()` 中故障为 `CHASSIS_FAULT_NONE`。
- 遥控链已经令`Chassis.enable_flag=1`。
- 安全输出路径仍保留。
- 已完成离线或低风险验证。

## 18. 当前框架的一句话总结

当前代码是一个按“公共配置 -> 驱动 -> 设备 -> 公共算法 -> 底盘模块 -> 任务调度”分层的安全骨架。它已经具备真实 CAN、SPI、USB CDC、BMI088、DM、DJI 的基础接口，包含 USB 流式协议、公共 CRC/PID/Kalman/LQR 和第一阶段轮腿控制器计算链，但所有电机输出默认仍保持安全零输出。
