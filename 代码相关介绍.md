# 代码相关介绍

本文档面向刚接手本工程的开发者，目标是让你先理解代码框架、模块边界和当前安全策略，再开始改代码。阅读代码前，请先阅读同目录下的 `代码风格要求.md`。

## 1. 工程定位

当前工程是基于 STM32H723 的轮腿底盘实机工程。工程主体仍由 STM32CubeMX 生成，用户代码集中放在 `User_File` 目录。

当前 `User_File` 是一个安全可编译的新框架，重点是把层级和接口先理清楚：

- 驱动层只负责硬件收发。
- 设备层只负责设备协议解析和设备状态维护。
- 底盘模块层只负责底盘输入输出抽象和故障判断。
- 任务层只负责 FreeRTOS 调度、模块连接和数据分发。
- 当前已经加入第一阶段轮腿控制器计算链，包括五连杆几何、虚拟腿长/腿角状态、腿长/横滚虚拟支撑力和 VMC 关节力矩映射。
- 当前不包含可用于实机闭环的 LQR 参数、轮电机非零电流换算、跳跃、打滑检测或完整运动闭环。
- 上电默认安全零输出；第一阶段即使控制器能计算中间量，默认也不会产生非零电机命令。

## 2. 顶层目录关系

工程中和用户代码最相关的文件如下：

```text
Wheel-legged/
├── CMakeLists.txt
├── 代码风格要求.md
├── 代码相关介绍.md
├── Core/
│   ├── Inc/
│   └── Src/
│       ├── main.c
│       ├── freertos.c
│       ├── fdcan.c
│       ├── spi.c
│       └── gpio.c
└── User_File/
    ├── 1_Middleware/
    │   ├── 0_Common/
    │   ├── 1_Driver/
    │   └── 2_Algorithm/
    ├── 2_Device/
    ├── 3_Chariot/
    ├── 4_Interaction/
    └── 5_Task/
```

### 2.1 `Core`

`Core` 是 CubeMX 生成层，主要提供 MCU、外设和 FreeRTOS 基础入口。

当前用户代码依赖的事实包括：

- `Core/Src/main.c` 初始化 GPIO、FDCAN1、FDCAN2、SPI2 和 FreeRTOS。
- `Core/Src/freertos.c` 在 `MX_FREERTOS_Init()` 中创建默认任务，并调用：
  - `CAN_Task_Init()`
  - `Chassis_Task_Init()`
  - `IMU_Task_Init()`
- `Core/Src/fdcan.c` 定义 `hfdcan1` 和 `hfdcan2`。
- `Core/Src/spi.c` 定义 `hspi2`。
- `Core/Inc/main.h` 定义 BMI088 片选和中断引脚：
  - `ACC_CS_Pin` / `ACC_CS_GPIO_Port`
  - `GYRO_CS_Pin` / `GYRO_CS_GPIO_Port`
  - `ACC_INT_Pin`
  - `GYRO_INT_Pin`

除非需要修改外设配置，否则不要直接改 CubeMX 生成代码。用户逻辑应优先放在 `User_File`。

### 2.2 `User_File`

`User_File` 是当前工程的用户代码根目录。它按职责分层：

```text
User_File/
├── 1_Middleware/
│   ├── 0_Common/
│   │   ├── app_config.h
│   │   └── app_status.h
│   ├── 1_Driver/
│   │   ├── FDCAN/
│   │   │   ├── driver_fdcan.c
│   │   │   └── driver_fdcan.h
│   │   └── SPI/
│   │       ├── driver_spi.c
│   │       └── driver_spi.h
│   └── 2_Algorithm/
│       ├── PID.c
│       └── PID.h
├── 2_Device/
│   ├── IMU/BMI088/
│   │   ├── device_bmi088.c
│   │   └── device_bmi088.h
│   └── Motor/
│       ├── Motor_DM/
│       │   ├── device_motor_dm.c
│       │   └── device_motor_dm.h
│       └── Motor_DJI/
│           ├── device_motor_dji.c
│           └── device_motor_dji.h
├── 3_Chariot/
│   └── 1_Module/Chassis/
│       ├── module_chassis.c
│       ├── module_chassis.h
│       ├── module_chassis_controller.c
│       ├── module_chassis_controller.h
│       ├── module_chassis_leg.c
│       ├── module_chassis_leg.h
│       ├── module_chassis_model.c
│       └── module_chassis_model.h
├── 4_Interaction/
└── 5_Task/
    ├── task_can.c
    ├── task_can.h
    ├── task_chassis.c
    ├── task_chassis.h
    ├── task_imu.c
    ├── task_imu.h
    ├── task_can_dispatch.c
    └── task_can_dispatch.h
```

`4_Interaction` 目前为空目录，后续可用于遥控器、上位机、裁判系统或人机交互相关模块。

## 3. 构建入口

`CMakeLists.txt` 当前只编译以下用户源文件：

```text
User_File/1_Middleware/1_Driver/FDCAN/driver_fdcan.c
User_File/1_Middleware/1_Driver/SPI/driver_spi.c
User_File/1_Middleware/2_Algorithm/PID.c
User_File/2_Device/IMU/BMI088/device_bmi088.c
User_File/2_Device/Motor/Motor_DM/device_motor_dm.c
User_File/2_Device/Motor/Motor_DJI/device_motor_dji.c
User_File/3_Chariot/1_Module/Chassis/module_chassis.c
User_File/3_Chariot/1_Module/Chassis/module_chassis_controller.c
User_File/3_Chariot/1_Module/Chassis/module_chassis_leg.c
User_File/3_Chariot/1_Module/Chassis/module_chassis_model.c
User_File/5_Task/task_can.c
User_File/5_Task/task_chassis.c
User_File/5_Task/task_imu.c
User_File/5_Task/task_can_dispatch.c
```

新增 `.c` 文件后，必须同步加入 `CMakeLists.txt` 的 `target_sources()`。新增头文件目录后，必须同步加入 `target_include_directories()`。

当前 include 路径已经包含：

```text
User_File/1_Middleware/0_Common
User_File/1_Middleware/1_Driver/FDCAN
User_File/1_Middleware/1_Driver/SPI
User_File/1_Middleware/2_Algorithm
User_File/2_Device/IMU/BMI088
User_File/2_Device/Motor/Motor_DM
User_File/2_Device/Motor/Motor_DJI
User_File/3_Chariot/1_Module/Chassis
User_File/5_Task
```

## 4. 分层职责总览

当前代码的核心原则是“下层不知道上层业务，上层不直接碰硬件细节”。

### 4.1 公共配置层：`1_Middleware/0_Common`

公共层保存跨模块共享的基础类型和集中配置。

#### `app_status.h`

定义统一返回类型 `app_status_t`：

```c
APP_STATUS_OK
APP_STATUS_ERROR
APP_STATUS_BUSY
APP_STATUS_TIMEOUT
APP_STATUS_INVALID_PARAM
APP_STATUS_NOT_READY
APP_STATUS_NO_RESOURCE
```

约定：

- 模块公共接口优先返回 `app_status_t`。
- `HAL_StatusTypeDef` 只在驱动内部使用。
- 上层不要直接依赖 HAL 错误类型。

#### `app_config.h`

集中定义硬件映射、任务周期、设备数量和超时阈值。

当前关键配置：

| 配置 | 当前值 | 含义 |
|---|---:|---|
| `APP_CONFIG_CAN_MAX_DATA_LENGTH` | `8U` | Classic CAN 最大数据长度 |
| `APP_CONFIG_CAN_TX_FRAME_CAPACITY` | `16U` | CAN 发送缓存槽数量 |
| `APP_CONFIG_CAN_RX_QUEUE_LENGTH` | `32U` | CAN 接收队列长度 |
| `APP_CONFIG_DM_MOTOR_COUNT` | `4U` | DM 髋关节电机数量 |
| `APP_CONFIG_DJI_WHEEL_COUNT` | `2U` | DJI 轮电机数量 |
| `APP_CONFIG_CAN_TASK_PERIOD_TICKS` | `1U` | CAN 任务周期 |
| `APP_CONFIG_IMU_TASK_PERIOD_TICKS` | `1U` | IMU 任务周期 |
| `APP_CONFIG_CHASSIS_TASK_PERIOD_TICKS` | `2U` | 底盘任务周期 |
| `APP_CONFIG_CHASSIS_CONTROLLER_OUTPUT_ENABLE` | `0U` | 控制器非零输出编译总开关，默认封锁 |
| `APP_CONFIG_DM_ONLINE_TIMEOUT_TICKS` | `50U` | DM 在线超时 |
| `APP_CONFIG_DJI_ONLINE_TIMEOUT_TICKS` | `50U` | DJI 在线超时 |
| `APP_CONFIG_CAN_TX_ERROR_LIMIT` | `1000U` | CAN 发送错误阈值 |

当前硬件映射：

| 设备 | 总线 | ID / 引脚 |
|---|---|---|
| 4 个 DM 髋关节电机 | FDCAN1 | `0x001`、`0x002`、`0x003`、`0x004` |
| 2 个 DJI 轮电机 | FDCAN2 | 反馈 `0x201`、`0x202`，控制 `0x200` |
| BMI088 | SPI2 | 片选来自 `main.h` 的 `ACC_CS` 和 `GYRO_CS` |

### 4.2 公共算法层：`1_Middleware/2_Algorithm`

公共算法层保存可以被多个业务模块复用的纯计算代码。算法层不直接访问 HAL、FreeRTOS、CAN、SPI 或电机设备状态，也不维护底盘业务故障位。

当前已有公共算法：

| 文件 | 职责 |
|---|---|
| `PID.h` / `PID.c` | 提供通用 PID 状态、PID 参数和基于反馈速度阻尼项的 PID 更新接口 |

`PID` 的使用方式：

- 调用方在自己的模块状态中保存 `algorithm_pid_state_t`。
- 调用方在自己的配置中保存 `algorithm_pid_config_t`。
- 公共 PID 只计算输出，不知道输出单位。
- 积分限幅和输出限幅由调用方按实际物理量配置。
- 当前底盘控制器的腿长控制和横滚控制都复用这一套 PID。

后续如果引入 Kalman、MPC、LQR 系数插值、低通滤波或矩阵运算等可复用算法，应优先放在本目录。不要为了预留结构创建空文件；只有当真实控制链路用到对应算法时，再新增完整实现并接入 `CMakeLists.txt`。

## 5. 驱动层

驱动层位于 `User_File/1_Middleware/1_Driver`。

驱动层只做硬件收发，不做业务解析。比如 FDCAN 驱动不知道 DM 电机和 DJI 电机，SPI 驱动不知道 BMI088 寄存器协议。

### 5.1 FDCAN 驱动：`driver_fdcan`

文件：

- `driver_fdcan.h`
- `driver_fdcan.c`

职责：

- 管理 FDCAN1 和 FDCAN2。
- 配置标准 ID 接收过滤器。
- 启动 FDCAN 外设。
- 发送标准 ID Classic CAN 数据帧。
- 在 HAL 接收回调中读取 FIFO 并转发给上层回调。

主要接口：

| 接口 | 作用 |
|---|---|
| `Driver_FDCAN_Init()` | 初始化 FDCAN 并注册接收回调 |
| `Driver_FDCAN_RegisterRxCallback()` | 更新接收回调 |
| `Driver_FDCAN_SendData()` | 发送一帧标准 ID Classic CAN 数据帧 |
| `Driver_FDCAN_DlcToLength()` | 将 HAL DLC 转为真实长度 |

接收流程：

```text
HAL_FDCAN_RxFifo0Callback()
└── Driver_FDCAN_DispatchRxFifo()
    ├── HAL_FDCAN_GetRxMessage()
    └── rxCallback(handle, frame)
```

驱动层接收回调类型是：

```c
typedef void (*driver_fdcan_rx_callback_t)(FDCAN_HandleTypeDef *handle,
                                        const driver_fdcan_rx_frame_t *frame);
```

当前上层注册的回调是 `CAN_Task_RxCallback()`。

### 5.2 SPI 驱动：`driver_spi`

文件：

- `driver_spi.h`
- `driver_spi.c`

职责：

- 在一次片选有效窗口内阻塞发送 SPI 数据。
- 在一次片选有效窗口内阻塞同步收发 SPI 数据。
- 只负责片选时序和 HAL SPI 调用。

主要接口：

| 接口 | 作用 |
|---|---|
| `Driver_SPI_Transmit()` | 拉低片选，阻塞发送，释放片选 |
| `Driver_SPI_TransmitReceive()` | 拉低片选，阻塞同步收发，释放片选 |

SPI 驱动不解释寄存器，不知道 BMI088 的 dummy 字节规则。BMI088 协议由设备层处理。

## 6. 设备层

设备层位于 `User_File/2_Device`。

设备层负责具体设备的协议解析、单位换算和状态维护。设备层不创建任务，不直接决定控制策略。

### 6.1 BMI088：`device_bmi088`

文件：

- `device_bmi088.h`
- `device_bmi088.c`

职责：

- 初始化 BMI088 加速度计和陀螺仪。
- 配置量程、输出频率和数据就绪中断。
- 阻塞读取加速度、角速度和温度。
- 将原始值换算为工程单位。
- 保存最近一次错误码。

当前配置：

| 部分 | 配置 |
|---|---|
| 加速度计 | `6G / 1600Hz` |
| 陀螺仪 | `2000dps / 230Hz` |
| 加速度单位 | `m/s^2` |
| 角速度单位 | `rad/s` |
| 温度单位 | 摄氏度 |

主要接口：

| 接口 | 作用 |
|---|---|
| `BMI088_Init()` | 初始化 BMI088 |
| `BMI088_Read()` | 读取一次完整 IMU 数据 |
| `BMI088_GetErrorCode()` | 获取最近错误码 |

注意事项：

- ACC 读寄存器协议是“读命令 + 1 字节 dummy + 数据”。
- GYRO 读寄存器协议是“读命令 + 数据”。
- 这两种时序差异在 `device_bmi088.c` 内处理，外部不需要关心。

### 6.2 DM 电机：`device_motor_dm`

文件：

- `device_motor_dm.h`
- `device_motor_dm.c`

职责：

- 管理 4 个 DM 髋关节电机。
- 保存每个电机的 CAN 总线、发送 ID、接收 ID 和安装方向。
- 解析 DM MIT 反馈。
- 按 SPR 工程写法将 DM MIT 力矩命令打包到 CAN 发送缓存。
- 维护安全输出、使能状态和在线状态。

当前 4 个 DM 电机索引：

```c
MOTOR_DM_LEFT_FRONT
MOTOR_DM_LEFT_BACK
MOTOR_DM_RIGHT_FRONT
MOTOR_DM_RIGHT_BACK
```

当前映射：

| 电机 | 总线 | TX ID | RX ID | 方向 |
|---|---|---:|---:|---:|
| 左前髋关节 | FDCAN1 | `0x002` | `0x002` | `1` |
| 左后髋关节 | FDCAN1 | `0x001` | `0x001` | `1` |
| 右前髋关节 | FDCAN1 | `0x004` | `0x004` | `-1` |
| 右后髋关节 | FDCAN1 | `0x003` | `0x003` | `-1` |

DM MIT 力矩命令字段：

| 字段 | 单位 | 当前安全输出 |
|---|---|---:|
| `torqueNm` | N*m | `0` |

当前发送帧采用 SPR 工程的简化力矩写法：

| 字节 | 内容 |
|---:|---|
| `data[0]` ~ `data[5]` | 固定为 `0x00` |
| `data[6]` | 12 bit 力矩编码高 8 位 |
| `data[7]` | 12 bit 力矩编码低 8 位 |

该写法只使用 DM MIT 的力矩通道，发送层不会下发位置、速度、KP、KD。

主要接口：

| 接口 | 作用 |
|---|---|
| `Motor_DM_Init()` | 初始化 4 个电机配置和状态 |
| `Motor_DM_UpdateFeedback()` | 解析一帧 DM 反馈 |
| `Motor_DM_SetCommand()` | 设置单个电机命令缓存 |
| `Motor_DM_GetState()` | 读取单个电机状态 |
| `Motor_DM_SetSafe()` | 设置安全输出标志 |
| `Motor_DM_SetEnable()` | 请求 DM 使能或失能特殊帧 |
| `Motor_DM_IsOnline()` | 根据反馈 tick 判断在线 |
| `Motor_DM_ZeroAll()` | 清空所有命令 |
| `Motor_DM_UpdateTxFrames()` | 将命令打包到 CAN 发送缓存 |

安全规则：

- `dmSafe = 1` 时，`Motor_DM_UpdateTxFrames()` 会强制把命令清零后再打包。
- `Motor_DM_ZeroAll()` 只清命令缓存，不直接发 CAN。
- 真正写入 CAN 发送缓存的是 `Motor_DM_UpdateTxFrames()`。
- 真正触发 CAN 发送的是 `CAN_Task_RequestTx()` 或 CAN 任务周期发送。

### 6.3 DJI 电机：`device_motor_dji`

文件：

- `device_motor_dji.h`
- `device_motor_dji.c`

职责：

- 解析 DJI ESC 反馈帧。
- 保存编码器、速度、电流、温度、反馈次数和在线状态。

反馈帧格式：

| 字节 | 含义 |
|---|---|
| `0..1` | 编码器原始值，大端 |
| `2..3` | 转速 `rpm`，大端有符号 |
| `4..5` | 电流原始值，大端有符号 |
| `6` | 温度，摄氏度 |
| `7` | 当前未使用 |

主要接口：

| 接口 | 作用 |
|---|---|
| `Motor_DJI_UpdateFeedback()` | 解析一帧 DJI 反馈 |
| `Motor_DJI_IsOnline()` | 根据反馈 tick 判断在线 |

DJI 电机的控制发送不在设备层完成，而由 `CAN_Task_SetDjiCurrent()` 打包 `0x200` 控制帧。

## 7. 底盘模块层

底盘模块位于 `User_File/3_Chariot/1_Module/Chassis`。

文件：

- `module_chassis.h`
- `module_chassis.c`
- `module_chassis_model.h`
- `module_chassis_model.c`
- `module_chassis_leg.h`
- `module_chassis_leg.c`
- `module_chassis_controller.h`
- `module_chassis_controller.c`

职责：

- 定义底盘控制模块的输入结构。
- 定义底盘控制模块的输出结构。
- 根据输入状态生成故障标志。
- 计算第一阶段轮腿控制器中间状态。
- 默认保持安全零命令，不直接打开实机闭环输出。

这层是后续接入 LQR、VMC、腿长控制和运动控制的主要入口。

### 7.0 控制器子模块

当前控制器按三个子模块拆开：

| 文件 | 职责 |
|---|---|
| `module_chassis_model.*` | 集中保存当前轮腿机器人这一套五连杆结构参数、轮参数、IMU 轴向映射、公共 PID 参数和输出开关 |
| `module_chassis_leg.*` | 根据两髋关节角度计算虚拟腿长、虚拟腿角、腿长速度、腿摆速度，并把虚拟支撑力/腿摆力矩映射为两个髋关节力矩 |
| `module_chassis_controller.*` | 组装控制状态量，调用公共 PID 计算腿长/横滚虚拟支撑力，调用五连杆 VMC 映射，生成受安全开关约束的电机输出 |

机械结构参数只支持当前这一套五连杆轮腿。后续如果仿真参数或实机尺寸变化，应优先修改 `module_chassis_model.c` 里的集中配置，不要在几何函数、任务层或电机设备层散落硬编码参数。

### 7.1 输入结构

`module_chassis_input_t` 包含：

- 外部使能状态 `isEnabled`
- IMU 状态，包括姿态角、去零偏陀螺角速度和最近一次积分周期
- 4 个 DM 电机状态
- 2 个 DJI 轮电机状态
- CAN 发送错误累计值

底盘模块不直接读取设备层全局变量，也不直接访问 HAL 句柄。任务层负责把设备状态转成 `module_chassis_input_t`。

### 7.2 输出结构

`module_chassis_output_t` 包含：

- 4 个 DM 电机命令
- 2 个 DJI 轮电流命令
- `safeOutput`
- `faultFlags`

当前 `Module_Chassis_Update()` 的行为：

```text
输入状态
├── 检查外部使能
├── 检查 IMU 初始化和姿态可用
├── 检查 DM 电机在线
├── 检查 DJI 电机在线
├── 检查 CAN 发送错误计数
├── 无故障时计算五连杆/控制器中间状态
├── 控制器异常时置 MODULE_CHASSIS_FAULT_CONTROLLER
└── 根据编译宏和模型输出开关决定是否解除安全输出
```

当前即使没有故障，也仍然保持零输出。这是有意设计，因为 IMU 轴向、电机方向、轮电机电流换算和 LQR 参数尚未完成实机确认。

### 7.3 故障标志

`module_chassis.h` 定义了以下故障位：

| 故障位 | 含义 |
|---|---|
| `MODULE_CHASSIS_FAULT_NONE` | 无故障 |
| `MODULE_CHASSIS_FAULT_DISABLED` | 底盘未使能 |
| `MODULE_CHASSIS_FAULT_IMU` | IMU 未就绪或有错误 |
| `MODULE_CHASSIS_FAULT_DM_MOTOR` | 任一 DM 电机离线 |
| `MODULE_CHASSIS_FAULT_DJI_MOTOR` | 任一 DJI 轮电机离线 |
| `MODULE_CHASSIS_FAULT_CAN` | CAN 发送错误累计超过阈值 |
| `MODULE_CHASSIS_FAULT_CONTROLLER` | 五连杆几何或控制器计算失败 |

### 7.4 第一阶段控制输出限制

控制器非零输出需要同时满足：

- `APP_CONFIG_CHASSIS_CONTROLLER_OUTPUT_ENABLE != 0`
- `chassisModelConfig->output.jointTorqueOutputEnabled != 0` 或 `wheelCurrentOutputEnabled != 0`
- 对应力矩/电流限幅参数大于 0
- 底盘故障为 `MODULE_CHASSIS_FAULT_NONE`

当前默认配置中上述开关均保持关闭，所以控制器只计算中间状态，最终下发仍为 DM 零力矩和 DJI 零电流。

### 7.5 当前控制状态量顺序

`module_chassis_model.h` 中定义的控制状态顺序与 Webots Python 控制器保持一致：

```text
s
dot_s
fai
dot_fai
theta_l
dot_theta_l
theta_r
dot_theta_r
theta_b
dot_theta_b
```

第一阶段 `s` 暂不积分累计，保持为 0；`dot_s` 根据轮速、腿长、腿角速度估算；LQR 增益矩阵默认全 0，后续应通过离线计算或拟合生成，不应在 STM32 上运行 Python/Scipy 风格的实时求解。

说明：早期 Webots 控制器里曾使用 `theta_ll/theta_lr` 作为左右腿角变量名，当前工程统一使用用户给出的 `theta_l/theta_r` 命名。旧变量名只作为历史代码对照，不再作为新框架状态量名称。

### 7.6 轮腿控制符号约定

本节记录已经通过 Webots 临时力矩测试确认的符号约定，后续离线求 LQR 增益、实机电机方向映射和调试日志都应遵守这里的定义。

用于确认力矩方向的 Webots 临时测试代码已经删除，测试结论保留在本文档和工程注释中。后续如果需要重新验证方向，应重新建立独立测试分支或临时脚本，不应把测试开关长期保留在主控制器里。

腿角定义：

```text
theta_l = legVerticalAngleOffsetRad - phi0_l + theta_b
theta_r = legVerticalAngleOffsetRad - phi0_r + theta_b
```

其中 `legVerticalAngleOffsetRad` 当前为 `pi/2`，`phi0_l/phi0_r` 是五连杆几何计算得到的虚拟腿相对机体系几何角，`theta_b` 是机体俯仰角。该定义与 Webots Python 控制器中的 `theta_ll = pi/2 - phi0_l + theta_b`、`theta_lr = pi/2 - phi0_r + theta_b` 保持一致。

已经确认的物理方向：

```text
theta_l > 0：左虚拟腿向机身后方倾斜
theta_r > 0：右虚拟腿向机身后方倾斜
dot_theta_l/dot_theta_r > 0：对应虚拟腿继续向机身后方摆动
```

控制输入顺序：

```text
T_l  = 左轮轮端力矩，单位 N*m
T_r  = 右轮轮端力矩，单位 N*m
T_pl = 左虚拟腿摆动力矩，单位 N*m
T_pr = 右虚拟腿摆动力矩，单位 N*m
```

已经确认的力矩方向：

```text
T_pl > 0：使 theta_l 增大，即驱动左虚拟腿向机身后方摆动
T_pr > 0：使 theta_r 增大，即驱动右虚拟腿向机身后方摆动
```

注意：这里定义的是控制层广义力矩方向，不等同于 DM 电机原始力矩方向。具体电机安装导致的正负号应在 `module_chassis_model.c` 的 `angleScale` 和 `torqueScale` 中处理，不允许在 LQR 增益、任务层或设备层里临时补反号。

## 8. 任务层

任务层位于 `User_File/5_Task`。

任务层负责 FreeRTOS 调度、模块连接和数据分发。任务层可以调用设备层和模块层，但不应该实现复杂控制算法。

### 8.1 CAN 任务：`task_can`

文件：

- `task_can.h`
- `task_can.c`

职责：

- 初始化 FDCAN1 和 FDCAN2 驱动。
- 提供 CAN 发送缓存表。
- 提供 CAN 接收消息队列。
- 将驱动层收到的报文转成 `task_can_rx_message_t`。
- 调用 `CAN_Task_RxMessageCallback()` 做应用层分发。
- 周期性发送所有已启用缓存帧。

主要内部对象：

| 对象 | 作用 |
|---|---|
| `canTxFrames[]` | 发送缓存槽 |
| `canTxMutex` | 保护发送缓存 |
| `canRxQueue` | 接收消息队列 |
| `canTxBusyCount` | 发送 FIFO 忙计数 |
| `canTxErrorCount` | 发送错误计数 |
| `canRxOverflowCount` | 接收队列溢出计数 |

CAN 接收流程：

```text
FDCAN 中断
└── driver_fdcan
    └── CAN_Task_RxCallback()
        ├── 写入 canRxQueue
        └── 设置 CAN_TASK_FLAG_RX

CanTask()
└── CAN_Task_DispatchRx()
    └── CAN_Task_RxMessageCallback()
        ├── FDCAN1 -> Motor_DM_UpdateFeedback()
        └── FDCAN2 -> Motor_DJI_UpdateFeedback()
```

CAN 发送流程：

```text
上层模块
├── CAN_Task_UpdateTxFrame()
│   └── 写入或更新 canTxFrames[]
└── CAN_Task_RequestTx()
    └── 设置 CAN_TASK_FLAG_TX

CanTask()
└── CAN_Task_SendFrames()
    └── Driver_FDCAN_SendData()
```

DJI 轮电机电流发送：

```text
CAN_Task_SetDjiCurrent()
└── 打包 ID 0x200 的 8 字节控制帧
    └── CAN_Task_UpdateTxFrame(&hfdcan2, 0x200, ...)
```

### 8.2 IMU 任务：`task_imu`

文件：

- `task_imu.h`
- `task_imu.c`

职责：

- 初始化 BMI088。
- 固定周期读取 BMI088。
- 启动阶段估计陀螺零偏。
- 用互补滤波维护基础 roll、pitch、yaw。
- 保存线程安全的 IMU 状态快照。

IMU 任务状态 `task_imu_state_t` 包含：

- BMI088 是否初始化
- 姿态是否准备好
- 初始化错误计数
- 读取错误计数
- 最近错误码
- BMI088 数据快照
- 陀螺零偏
- roll、pitch、yaw
- 最近姿态积分周期

当前姿态估计说明：

- roll 和 pitch 使用陀螺积分，并用加速度低频修正。
- yaw 只做陀螺积分。
- 当前没有磁力计或外部航向观测，因此 yaw 不能视为长期绝对航向。
- 完成 `APP_CONFIG_IMU_BIAS_SAMPLE_COUNT` 次零偏采样后，`isAttitudeReady` 才会置 1。

### 8.3 底盘任务：`task_chassis`

文件：

- `task_chassis.h`
- `task_chassis.c`

职责：

- 初始化 DM 电机设备层。
- 初始化底盘模块。
- 周期性收集 IMU、电机、CAN 状态。
- 调用 `Module_Chassis_Update()`。
- 将底盘模块输出下发给 DM 和 DJI。
- 对外提供使能接口和故障查询接口。

底盘任务主流程：

```text
ChassisTask()
├── Motor_DM_Init()
├── Motor_DM_SetEnable(0)
├── Module_Chassis_Init()
└── while (1)
    ├── Chassis_Task_BuildInput()
    │   ├── IMU_Task_GetState()
    │   ├── Motor_DM_GetState()
    │   ├── Motor_DM_IsOnline()
    │   ├── Motor_DJI_IsOnline()
    │   └── CAN_Task_GetTxErrorCount()
    ├── Module_Chassis_Update()
    ├── Chassis_Task_ApplyOutput()
    │   ├── Motor_DM_SetSafe()
    │   ├── Motor_DM_ZeroAll()
    │   ├── Motor_DM_SetCommand()
    │   ├── CAN_Task_SetDjiCurrent()
    │   ├── Motor_DM_UpdateTxFrames()
    │   └── CAN_Task_RequestTx()
    └── osDelayUntil()
```

外部可用接口：

| 接口 | 作用 |
|---|---|
| `Chassis_Task_Init()` | 创建底盘任务 |
| `Chassis_Task_SetEnable()` | 设置底盘使能状态 |
| `Chassis_Task_IsEnabled()` | 查询底盘使能状态 |
| `Chassis_Task_GetFaultFlags()` | 查询底盘故障位 |
| `Chassis_Task_NotifyImuReady()` | 兼容旧调用链，当前固定周期调度中不使用 |

注意：`Chassis_Task_SetEnable(1)` 目前只允许后续控制算法有机会产生非零输出。由于 `Module_Chassis_Update()` 当前仍固定安全零输出，所以调用该接口不会让电机产生非零力矩。

### 8.4 应用分发：`task_can_dispatch`

文件：

- `task_can_dispatch.h`
- `task_can_dispatch.c`

职责：

- 定义 DJI 轮电机反馈数组 `chassisDjiWheels[]`。
- 实现 `CAN_Task_RxMessageCallback()`。
- 根据 CAN 总线和 ID 把接收帧分发到对应设备层。

当前分发规则：

| 总线 | 条件 | 分发目标 |
|---|---|---|
| FDCAN1 | 长度为 DM 帧长度 | `Motor_DM_UpdateFeedback()` |
| FDCAN2 | ID `0x201` | `Motor_DJI_UpdateFeedback(&chassisDjiWheels[0])` |
| FDCAN2 | ID `0x202` | `Motor_DJI_UpdateFeedback(&chassisDjiWheels[1])` |

## 9. 启动流程

系统启动大致流程如下：

```text
main()
├── HAL_Init()
├── SystemClock_Config()
├── MX_GPIO_Init()
├── MX_FDCAN1_Init()
├── MX_FDCAN2_Init()
├── MX_SPI2_Init()
├── osKernelInitialize()
├── MX_FREERTOS_Init()
│   ├── defaultTask
│   ├── CAN_Task_Init()
│   ├── Chassis_Task_Init()
│   └── IMU_Task_Init()
└── osKernelStart()
```

任务创建后：

```text
CanTask
├── Driver_FDCAN_Init(FDCAN1)
└── Driver_FDCAN_Init(FDCAN2)

ChassisTask
├── Motor_DM_Init()
├── Motor_DM_SetEnable(0)
└── Module_Chassis_Init()

ImuTask
└── BMI088_Init()
```

## 10. 运行期数据流

### 10.1 传感器数据流

```text
BMI088 硬件
└── SPI2
    └── driver_spi
        └── device_bmi088
            └── task_imu
                └── task_imu_state_t
                    └── task_chassis
                        └── module_chassis_input_t
```

### 10.2 电机反馈数据流

```text
DM/DJI 电机反馈
└── FDCAN
    └── driver_fdcan
        └── task_can
            └── CAN_Task_RxMessageCallback()
                ├── device_motor_dm
                └── device_motor_dji
```

### 10.3 控制输出数据流

```text
task_chassis
└── module_chassis
    └── module_chassis_output_t
        ├── Motor_DM_SetCommand()
        ├── Motor_DM_UpdateTxFrames()
        ├── CAN_Task_SetDjiCurrent()
        └── CAN_Task_RequestTx()
            └── task_can
                └── driver_fdcan
                    └── FDCAN 硬件
```

## 11. 当前安全策略

当前框架默认面向实机安全。

核心规则：

- 上电默认 `chassisEnable = 0`。
- DM 电机默认 `dmSafe = 1`。
- 底盘模块默认 `safeOutput = 1`。
- 底盘任务每个周期都下发零输出。
- 设备未就绪或故障时继续覆盖零输出，而不是停止任务。
- 控制器模块可以计算中间控制量，但默认没有任何路径允许非零底盘控制命令下发。

故障来源包括：

- 底盘未使能。
- IMU 未初始化。
- IMU 姿态未准备好。
- IMU 最近错误码非零。
- 任一 DM 电机离线。
- 任一 DJI 轮电机离线。
- CAN 发送错误计数超过阈值。
- 五连杆几何或控制器计算失败。

为什么故障时仍然持续发送零输出：

- 如果停止发送，电机或发送缓存可能保留上一帧命令。
- 持续覆盖零命令可以降低旧命令残留风险。
- 这是重新搭框架阶段更保守的行为。

## 12. 当前没有实现或未启用的内容

以下能力当前没有实现：

- 轮速闭环。
- 跳跃。
- 落地检测。
- 打滑检测。
- 遥控器或上位机命令解析。
- 功率限制。
- 完整急停输入链路。

以下能力已经有第一阶段计算链，但默认未启用实机非零输出：

- 五连杆正运动学和速度计算。
- VMC 虚拟支撑力/腿摆力矩到髋关节力矩映射。
- 基于公共 `PID` 的腿长 PID 虚拟支撑力。
- 基于公共 `PID` 的横滚 PID 虚拟支撑力差。
- 控制状态向量组装。

这些功能后续应接入到明确的层级里，不要直接塞进驱动层或设备层。

## 13. 后续扩展建议

### 13.1 启用 LQR 和非零输出

建议入口：

```text
User_File/3_Chariot/1_Module/Chassis/module_chassis_controller.c
User_File/3_Chariot/1_Module/Chassis/module_chassis_model.c
```

推荐步骤：

1. 台架确认 IMU 的 roll、pitch、yaw 和 gyro 正负方向。
2. 台架确认 4 个 DM 髋关节电机的零位、方向和力矩方向。
3. 根据当前机械参数离线生成 LQR 增益或拟合系数，不在 STM32 上实时求解 LQR。
4. 确认 DJI 轮电机型号、减速比和轮端力矩到原始电流的换算关系。
5. 在 `module_chassis_model.c` 中填写增益、限幅和输出开关。
6. 在 `app_config.h` 中打开 `APP_CONFIG_CHASSIS_CONTROLLER_OUTPUT_ENABLE`。
7. 在 `Module_Chassis_Update()` 中只在以下条件同时满足时生成非零输出：
   - `input->isEnabled != 0`
   - `faultFlags == MODULE_CHASSIS_FAULT_NONE`
   - 控制参数已经标定
   - 输出经过限幅
8. 保留 `safeOutput` 路径。
9. 不要在驱动层、设备层或任务层直接写控制算法。

当前建议控制链路：

```text
设备状态
└── task_chassis 组装输入
    └── Module_Chassis_Update()
        ├── module_chassis_controller
        ├── module_chassis_leg
        ├── module_chassis_model
        └── 安全输出切换
```

### 13.2 新增设备

新增设备时按以下顺序做：

1. 在 `2_Device` 下建立设备模块。
2. 设备模块只负责协议解析和状态维护。
3. 如需硬件收发，优先复用 `driver_fdcan` 或 `driver_spi`。
4. 在任务层添加调度或分发。
5. 必要时在 `app_config.h` 中集中添加 ID、数量和超时。
6. 更新 `CMakeLists.txt`。

不要让设备层创建 FreeRTOS 任务，除非这个设备确实需要独立调度，并且已经明确设计边界。

### 13.3 新增遥控器或上位机

建议放在：

```text
User_File/4_Interaction/
```

推荐职责：

- 解析遥控器或上位机协议。
- 输出统一的底盘命令结构。
- 不直接写电机电流。
- 不直接绕过 `Module_Chassis_Update()`。

后续可让 `task_chassis` 同时收集：

- IMU 状态
- 电机状态
- CAN 状态
- 人机交互命令

然后统一传入底盘模块。

### 13.4 新增故障处理

新增故障时优先在 `module_chassis.h` 中增加故障位，例如：

```c
#define MODULE_CHASSIS_FAULT_REMOTE 0x00000040UL
```

然后在 `Module_Chassis_CheckFaults()` 中集中判断。

不要在多个任务中分别维护同一类故障事实。底盘故障最终应汇总到 `chassisFaultFlags`。

## 14. 新人推荐阅读顺序

第一次接手代码时，建议按这个顺序读：

1. `代码风格要求.md`
2. `代码相关介绍.md`
3. `CMakeLists.txt`
4. `Core/Src/freertos.c`
5. `User_File/1_Middleware/0_Common/app_config.h`
6. `User_File/1_Middleware/0_Common/app_status.h`
7. `User_File/1_Middleware/2_Algorithm/PID.c`
8. `User_File/5_Task/task_can.c`
9. `User_File/5_Task/task_can_dispatch.c`
10. `User_File/2_Device/Motor/Motor_DM/device_motor_dm.c`
11. `User_File/2_Device/Motor/Motor_DJI/device_motor_dji.c`
12. `User_File/2_Device/IMU/BMI088/device_bmi088.c`
13. `User_File/5_Task/task_imu.c`
14. `User_File/5_Task/task_chassis.c`
15. `User_File/3_Chariot/1_Module/Chassis/module_chassis.c`
16. `User_File/3_Chariot/1_Module/Chassis/module_chassis_model.c`
17. `User_File/3_Chariot/1_Module/Chassis/module_chassis_leg.c`
18. `User_File/3_Chariot/1_Module/Chassis/module_chassis_controller.c`

如果你只想看主数据流，优先读：

```text
freertos.c
└── task_can.c
└── task_imu.c
└── task_chassis.c
    └── module_chassis.c
        └── module_chassis_controller.c
```

## 15. 修改代码前检查清单

每次改代码前先确认：

- 是否已经阅读 `代码风格要求.md`。
- 要改的是驱动、设备、模块还是任务层。
- 这个改动是否会让下层依赖上层业务。
- 是否新增了未使用函数、变量、宏或中间状态。
- 是否需要同步 `CMakeLists.txt`。
- 是否需要把硬件 ID、数量或超时放入 `app_config.h`。
- 是否破坏默认安全零输出。
- 是否需要新增或更新本文档。

## 16. 验证建议

基础验证命令：

```powershell
cmake --build build/Debug
```

如果 `cmake --build` 在本机环境卡住，可直接调用 CMake 缓存中的 Ninja。当前工程缓存中的构建器路径可在 `build/Debug/CMakeCache.txt` 的 `CMAKE_MAKE_PROGRAM` 查看。

示例：

```powershell
& 'C:\Users\25697\AppData\Local\stm32cube\bundles\ninja\1.13.2+st.1\bin\ninja.exe' -C 'E:\AWL_WORK\Wheel-legged\Wheel-legged\build\Debug' -d explain Wheel-legged
```

静态检查建议：

```powershell
rg -n "drv_|dvc_|mod_|motor_task|daemon_task|task_config_and_callback" User_File CMakeLists.txt Core\Src\freertos.c
rg -n "HAL_StatusTypeDef .*\\(" User_File
rg -n "TODO|Mock|Stub|Demo" User_File
rg -n "module_chassis_pid|UpdatePid|pid_state_t" User_File
```

期望结果：

- 构建成功或 Ninja 显示 `no work to do`。
- 旧任务名不应再出现在有效用户代码和构建入口中。
- 模块公共接口不应直接返回 `HAL_StatusTypeDef`。
- 不应出现 Mock、Stub 或 Demo 级替代逻辑。

## 17. 常见问题

### 为什么 `Module_Chassis_Update()` 现在总是零输出？

这是当前框架阶段的安全设计。控制算法尚未接入，不能让实机产生未经验证的非零输出。

### 为什么设备层不直接发电机命令？

设备层只维护设备状态和协议格式。是否发命令、何时发命令、发什么命令由任务层和底盘模块决定。

### 为什么硬件 ID 放在 `app_config.h`？

硬件映射是全局事实，集中配置可以避免多个文件各自维护一份 ID。

### 为什么驱动层仍然使用 HAL？

驱动层本来就直接面对硬件库。区别是 HAL 返回值不向设备层和任务层扩散，公共接口统一转成 `app_status_t`。

### 什么时候可以让电机非零输出？

至少需要满足：

- 控制算法有明确设计依据。
- 电机方向、单位、限幅已核对。
- `Module_Chassis_Update()` 中故障为 `MODULE_CHASSIS_FAULT_NONE`。
- 外部使能明确打开。
- 安全输出路径仍保留。
- 已完成离线或低风险验证。

## 18. 当前框架的一句话总结

当前代码是一个按“公共配置 -> 驱动 -> 设备 -> 底盘模块 -> 任务调度”分层的安全骨架。它已经具备真实 CAN、SPI、BMI088、DM、DJI 的基础接口、状态链路和第一阶段轮腿控制器计算链，但所有电机输出默认仍保持安全零输出。
