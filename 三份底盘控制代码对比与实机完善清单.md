# 三份底盘控制代码对比与实机完善清单

## 1. 文档目的

本文对比以下三份代码：

1. 当前 STM32 实机工程：`E:\AWL_WORK\Wheel-legged\Wheel-legged`
2. MuJoCo 仿真工程：`E:\AWL_WORK\code\code\wheel-legged\Simulate`
3. SPR 实机参考工程：`E:\AWL_WORK\Wheel-legged\Others\SPR Balanced-infantry\SPR Balanced-infantry\code\chassis\chassis`

分析目标不是选一份代码整体替换当前工程，而是明确三者各自负责什么：

- 当前工程作为最终实机代码，保留十维整车建模、双腿长 K 矩阵和现有 Debug/参数配置方式。
- MuJoCo 工程用于验证十维模型、状态符号、目标生成、LQR、VMC 和输出限幅。
- SPR 工程用于参考已经过实机运行的任务流程、模式管理、支撑力、防劈叉、离地处理和功率限制。

SPR 的六状态模型、K 矩阵、PID、机械零位、KNN 数据和经验补偿参数不能直接复制到当前工程。

## 2. 总体结论

三份代码没有一份可以单独覆盖当前全部需求：

| 工程 | 最主要价值 | 当前局限 |
|---|---|---|
| 当前 STM32 工程 | 十维整车 LQR、左右腿长二维 K 拟合、真实 IMU/CAN/电机接口、集中 Debug | 实机参数没有闭合，控制目标和多数模式未实现，非零输出仍被锁住 |
| MuJoCo 工程 | 验证十维模型、符号、目标斜坡、yaw 环绕和 VMC 输出分配 | 不包含真实噪声、延迟、掉线、功率和机械保护；当前源码还有编译缺陷 |
| SPR 工程 | 已验证的实机控制流程和完整功能链 | 模型不同、硬编码多、全局耦合重，部分算法和参数只适用于原车 |

当前工程不是只缺一个控制公式。基础的十维 `LQR + VMC + Kalman + IMU + CAN` 链路已经存在，但距离可进行实机平衡还缺少：

1. 与本车一致的机械和动力学参数。
2. 六个电机的零位、反馈极性和输出极性标定。
3. 实际可用的使能、模式和目标输入链路。
4. 支撑力、输出限幅和保护参数的实机标定。
5. 站立以外模式和离地、功率等实机控制能力。

## 3. 三份代码的控制流程

### 3.1 当前 STM32 工程

任务入口位于 `User_File/5_Task/chassis_task.c`，当前每轮流程是：

```text
读取 IMU、DM、DJI、CAN 反馈
    -> 根据 enabled 和 mode 选择 state
        -> STANDING 执行 chassis_control_loop()
        -> 其他 state 全部执行 chassis_zero_output()
            -> 把命令写入 DM 和 DJI 的 CAN 发送缓存
```

`chassis_control_loop()` 的主要顺序是：

```text
检查 IMU、电机、CAN 和配置
    -> 五连杆正运动学与速度解算
        -> 轮速、腿部运动和 IMU 加速度组成速度观测
            -> Kalman 融合速度
                -> 集中生成十维状态
                    -> 腿长 PID、roll PID 和前馈生成支撑力
                        -> 按左右腿长计算 4x10 K 矩阵
                            -> 计算四路 LQR 输出
                                -> VMC 映射到四个关节力矩
                                    -> 轮力矩转电流并限幅
```

该流程已经覆盖基础平衡控制所需的数学链路，但当前存在三个直接阻止非零输出的条件：

- `chassis_set_enable()` 和 `chassis_set_mode()` 在工程中没有调用者，`chassis.enabled` 上电后保持 0。
- `APP_CONFIG_CHASSIS_CONTROLLER_OUTPUT_ENABLE` 为 0。
- 关节、轮输出开关、力矩限幅和轮力矩转电流系数均为 0。

因此当前代码可以计算部分反馈和中间量，但默认不会驱动底盘电机。

### 3.2 MuJoCo 仿真工程

主控制流程位于 `Controller/main.cpp` 和 `Controller/controller.cpp`：

```text
从 MuJoCo 读取关节、车体姿态和运动反馈
    -> 按键输入生成速度和 yaw 角速度指令
        -> 限加速度生成目标速度
            -> 积分得到目标位移和目标 yaw
                -> controllerStep()
                    -> 五连杆、速度融合、十维状态、双腿长 K、LQR、PID、VMC
                        -> 写入六个 MuJoCo 执行器
                            -> 推进一个 2 ms 物理周期
```

MuJoCo 和当前工程的核心共同点：

- 状态顺序相同：`s, dot_s, yaw, dot_yaw, theta_l, dot_theta_l, theta_r, dot_theta_r, pitch, dot_pitch`。
- 输出都是左轮、右轮、左腿摆、右腿摆四路广义力矩。
- 控制律都是 `u = K * (target - state)`。
- K 都由左右腿长共同决定，而不是左右腿分别使用完全独立的六状态模型。
- 五连杆正运动学、速度求导、腿长/roll PID 和基础 VMC 公式基本对应。

MuJoCo 比当前 STM32 控制链多出的关键内容：

- 速度和 yaw 角速度目标有斜坡限制。
- 目标位移和目标 yaw 由目标速度积分生成。
- yaw 误差使用跨越 `+pi/-pi` 的最短角度。
- VMC 优先保留支撑力，剩余关节力矩能力再分配给腿摆控制。
- 腿长误差过大时逐步削弱腿摆力矩，防止支撑和摆腿同时抢占关节能力。
- PID 复位时会同时清除腿长 PID、roll PID 和运动估计状态。

MuJoCo 当前源码存在可复现性问题：

- `Controller/main.cpp` 在 `runOneSimulationStep()` 内调用 `mj_step(simulation->model, simulation->data)`，当前作用域没有 `simulation` 变量；正确接口 `mujocoStepSimulation(&runtime->simulation)` 被注释。
- 现有 `build/debug` 的缓存记录的是另一台 Linux 环境路径，不能用现存旧可执行文件证明当前源码仍可构建。
- 历史仿真结果可以说明控制思路基本可行，但在修复上述源码问题并重新构建前，不能把当前 checkout 认定为可重复验证状态。

### 3.3 SPR 实机工程

SPR 的 `Task/chassis_task.c` 使用固定 1 ms 循环，主流程是：

```text
chassis_set_mode()
    -> chassis_feedback_update()
        -> chassis_set_contorl()
            -> chassis_control_loop()
                -> 根据掉线和模式发送轮、关节电机命令
```

`chassis_feedback_update()` 主要完成：

- 读取六个电机反馈。
- 按硬编码零位转换左右腿主动杆角度。
- 调用 MATLAB 生成的 `leg_position()` 和 `leg_speed()`。
- 生成腿角、机体 pitch、速度和位移六状态反馈。
- 使用二维速度/加速度 Kalman 融合。
- 估算左右支持力，供离地相关逻辑使用。

`chassis_control_loop()` 主要完成：

```text
计算左右腿支撑力 F
    -> 用平均腿长计算左右 K[12]
        -> 计算 yaw 附加轮力矩
            -> 按状态机选择站立、零力矩、倒地、自救、小板凳或上台阶
                -> LQR 计算轮力矩和腿摆力矩
                    -> 轮力矩转电流并进行功率限制
                        -> 双腿协同 PD 防劈叉
                            -> KNN 离地检测及离地控制降级
                                -> leg_force() 映射四个关节力矩
```

SPR 已实现的功能包括：

- 遥控器/双板命令切换底盘模式。
- 六电机上线等待和运行掉线保护。
- 正常站立、零力矩、倒地、自救、小板凳和上台阶。
- 腿长 PID、重力前馈、roll 补偿、跳跃力、气弹簧和离心力补偿。
- 跳跃状态机。
- 双腿协同 PD 防劈叉。
- 支持力估算、KNN 离地识别和离地后的轮输出清零。
- 轮电机功率预测和功率限制。

## 4. 状态模型和 K 矩阵差异

### 4.1 当前工程和 MuJoCo：十维整车模型

```text
x = [s, dot_s, yaw, dot_yaw,
     theta_l, dot_theta_l,
     theta_r, dot_theta_r,
     pitch, dot_pitch]

u = [left_wheel, right_wheel, left_leg_swing, right_leg_swing]
```

特点：

- 左右腿、左右轮、yaw 和机体 pitch 在同一个模型内耦合。
- K 是 `4x10` 矩阵。
- 每个 K 元素使用左右腿长的 `poly22`：

```text
K = p00 + p10*L_left + p01*L_right
        + p20*L_left^2 + p11*L_left*L_right + p02*L_right^2
```

- 左右腿长度不同时，K 能表达整车不对称状态。

### 4.2 SPR：每腿六状态模型

SPR 每侧控制使用相同的六状态定义：

```text
x_leg = [theta, dot_theta, x, dot_x, pitch, dot_pitch]
u_leg = [wheel_torque, leg_swing_torque]
```

特点：

- 每条腿保存 `K[12]`，偶数项用于轮力矩，奇数项用于腿摆力矩。
- `Leg_L` 是左右腿实际长度平均值。
- `LQR_K()` 使用 `L, L^2, L^3` 一维三次多项式生成 K。
- 左右两侧调用时输入相同的平均腿长，因此获得相同 K，左右差异主要靠输出符号处理。
- yaw 不在 LQR 状态中，而是单独计算 `Yaw_turn` 后叠加到轮力矩。

### 4.3 不能直接互换的原因

SPR 的 `K[12]` 不能填入当前工程的 `4x10 lqr_k`：

- 状态数量不同。
- 状态物理含义和顺序不同。
- 输出数量不同。
- yaw 在模型内外的位置不同。
- SPR 只按平均腿长调度，当前模型同时使用左右腿长。
- 两套模型的质量、惯量、轮半径、采样周期和执行器定义不同。

当前工程应继续保留十维整车模型，不应为了模仿 SPR 的代码形式退回六维模型。

## 5. 机械参数和离线模型差异

### 5.1 当前已发现的不一致

| 参数 | 当前 STM32 | MuJoCo 控制器 | 当前 `ABK_LQR.m` |
|---|---:|---:|---:|
| 主动杆 `link1/link4` | `0.215 m` | `0.215 m` | 需要按离线模型重新核对 |
| 中间杆 `link2/link3` | `0.258 m` | `0.258 m` | 需要按离线模型重新核对 |
| 前后髋轴距离 | `0 m` | `0 m` | 需要填实际值 |
| 轮半径 | `0.10 m` | `0.058 m` | `0.0775 m` |
| K 工作范围 | 配置为 `0.15~0.35 m` | `0.18~0.30 m` | 原始腿参数表为 `0.11~0.30 m` |

即使两份控制器源代码中的四根连杆长度相同，也不能说明整车模型相同。MuJoCo XML 中的质量、惯量、轮半径、关节轴、质心和执行器定义仍与当前实机不同。

### 5.2 当前 K 的主要风险

当前固件中的 K 系数来自 `ABK_LQR.m` 的 `poly22` 拟合，但存在以下问题：

- MATLAB 里的质量、转动惯量、质心和轮参数没有证明与当前实机一致。
- 原始腿部参数表只覆盖 `0.11~0.30 m`，固件允许 K 输入到 `0.35 m`，因此 `0.30~0.35 m` 属于外推。
- 固件当前默认使用固定左右腿长 `0.25 m` 计算 K，没有使用实时腿长。
- 离线模型轮半径、固件轮半径和 MuJoCo 轮半径互不一致。

在实际参数重新进入 MATLAB 并完成离线稳定性检查前，当前 K 只能作为控制链调试数据，不能直接认定为可上车平衡参数。

## 6. 速度融合和 IMU 对比

### 6.1 速度融合

三份代码都采用轮速/腿运动学速度和 IMU 前向加速度构造速度估计。

当前工程和 SPR 的 Kalman 参数基本相同：

```text
state = [velocity, acceleration]
Q = diag(0.1, 0.1)
R = diag(100, 1e12)
```

`R[1][1] = 1e12` 表示滤波器几乎不相信加速度测量，因此当前效果更接近对运动学速度的平滑，而不是强融合加速度。

当前工程有一个明显的位移状态风险：

- 只有 `abs(forward_speed) <= 0.1 m/s` 才积分 `s`。
- 超过该速度会把 `s` 直接清零。
- MuJoCo 对应阈值为 `5 m/s`。

这会导致当前工程稍微移动后位置状态反复清零，破坏 `s` 状态的连续性。

### 6.2 IMU

当前工程和 SPR 都不是 Mahony，而是四元数 EKF，并都包含 BMI088 温控。

当前工程相对 SPR 的结构优势：

- 温控、启动静态零偏、EKF 参数和 Debug 集中在 IMU 任务中。
- 有启动阶段陀螺仪零偏采样。
- 温度稳定且机体静止时，单独慢速修正 Z 轴残余零偏。
- 输出统一转换到右手系：X 前、Y 左、Z 上。
- 全局 `imuTaskDebugState` 便于长期监视。

SPR 的优势是其 BMI088、温控参数、坐标转换和 EKF 已在原车上运行过，但其安装方向和固定参数只适用于原板卡安装。

两份代码共同的限制：

- 没有磁力计、视觉航向、双天线 GNSS 或其他绝对 yaw 观测。
- 加速度计只能约束 roll 和 pitch，不能长期约束 yaw。
- 温控和零偏学习只能降低 yaw 漂移，不能消除长期绝对航向漂移。

当前十维 LQR 直接使用 yaw，因此上电进入控制时至少需要把目标 yaw 对齐当前 yaw，并使用最短角度误差。若需要长期绝对航向，还必须引入外部航向观测。

## 7. 支撑力、VMC 和输出分配

### 7.1 当前 STM32

当前支撑力包含：

- 左右腿长 PID。
- 基础支撑前馈。
- roll PID 左右差动补偿。

当前 VMC 将支撑力和 LQR 腿摆力矩直接映射到关节，再对每个关节统一限幅。存在的风险是：

- 支撑和腿摆可能同时要求较大关节力矩。
- 统一截断后无法保证维持腿长的支撑分量优先。
- 腿长误差很大时，LQR 腿摆仍可能与腿长 PID 抢占关节能力。

### 7.2 MuJoCo

MuJoCo 已实现：

- 先计算支撑力占用的关节力矩。
- 使用关节剩余力矩能力分配腿摆控制。
- 腿长误差从 `0.01 m` 增加到 `0.05 m` 时，逐渐把腿摆控制降到 0。

这部分与当前十维控制链兼容，后续应优先参考 MuJoCo，而不是复制 SPR 的硬编码限幅。

### 7.3 SPR

SPR 的支撑力功能最完整，包含腿长 PID、重力前馈、roll、跳跃、气弹簧和离心力补偿，但公式中大量使用原车质量、几何、气弹簧和经验参数。

可复用的是计算顺序和功能边界，不可复用的是数值和符号。

## 8. 模式、状态机和实机功能差异

| 功能 | 当前 STM32 | MuJoCo | SPR |
|---|---|---|---|
| 零力矩 | 已实现 | `zero` 模式 | 已实现 |
| 支撑但不平衡 | 没有独立实现 | `support` 模式 | 可通过小板凳/位置控制调试 |
| 正常站立 | 基础计算已实现 | 已仿真 | 已实机使用 |
| 速度目标 | 固定全零 | 限加速度生成 | 遥控/双板输入 |
| yaw 目标 | 固定全零 | 限加速度并积分 | LQR 外单独转向闭环 |
| 板凳模式 | 枚举存在，实际零输出 | 无对应状态机 | 已实现 |
| 倒地/重新站立 | 枚举存在，实际零输出 | 未实现 | 已实现 |
| 防劈叉 | 未实现 | 未实现 | 已实现 |
| 离地检测 | 未实现 | 由仿真接触可观察，未形成实机检测器 | KNN + 支持力相关逻辑 |
| 跳跃/落地 | 未实现 | 未形成完整状态机 | 已实现经验状态机 |
| 上台阶 | 未实现 | 未实现 | 已实现经验状态机 |
| 功率限制 | 未实现 | 不包含真实电源模型 | 已实现轮电机功率预测和限制 |

当前 `CHASSIS_FALLEN`、`CHASSIS_FALLING_TO_STAND` 和 `CHASSIS_BENCH` 只是名称和分支，最终都调用 `chassis_zero_output()`。它们不能被描述为已经实现。

## 9. 三份代码的优缺点

### 9.1 当前 STM32 工程

优点：

- 十维整车模型能表达左右腿、yaw 和 pitch 的耦合。
- 双腿长 `poly22` 比平均腿长一维 K 更适合左右腿长不同的情况。
- 控制参数集中在 `chassis_config.c`，便于长期调试。
- `chassis` 是统一的全局 Watch 入口，中间量可直接观察。
- IMU、Kalman、VMC、LQR、设备和任务边界较清楚。
- 已有 IMU/电机离线检查、零输出、力矩限幅和安全发送基础链路。
- 当前 STM32 工程可以成功完成现有 Debug 构建。

缺点：

- 机械、动力学和执行器参数尚未与本车闭合。
- 使能和模式 API 没有实际输入调用者。
- 只有站立控制有计算，其余模式均为空功能。
- 目标状态全为常量 0，没有速度、位置、yaw 和腿长命令更新。
- yaw 没有最短角度误差和上电目标对齐。
- 控制任务约 2 ms，但部分计算使用 IMU 约 1 ms 的 `dt`。
- 位移积分阈值 `0.1 m/s` 太低。
- PID 在模式退出时没有统一清零，重新进入站立可能带入旧积分。
- 支撑力和腿摆没有执行器能力分配。
- 没有机械软限位、过温/过流策略、功率限制和离地降级。
- 大量逐项展开的 LQR 乘法便于观察，但重复代码较多，后续修改状态顺序时容易漏改。

### 9.2 MuJoCo 工程

优点：

- 与当前工程使用同一套十维状态和四路 LQR 输出定义。
- 已验证基础平衡、移动和变腿长控制思路。
- 目标生成、yaw 环绕、PID 复位和输出分配比当前固件完整。
- 有控制器测试和详细日志，可用于回归状态顺序、K、VMC 和符号。
- 仿真中可以安全验证极性、限幅和扰动响应。

缺点：

- MuJoCo 反馈接近理想值，没有真实编码器量化、CAN 延迟、丢包和电机内环差异。
- 没有电池、超级电容、电机温升和真实功率限制。
- 机械参数不是当前实机参数。
- 轮胎接触、摩擦和结构间隙与实机不完全一致。
- 当前源码存在 `simulation` 未定义问题，已有旧二进制不能替代重新构建验证。

### 9.3 SPR 工程

优点：

- 控制流程已经在实机环境中运行。
- 主循环顺序直接，现场调试时容易沿流程查看变量。
- 模式、保护、支撑、离地、跳跃、自救、上台阶和功率链路完整。
- 防劈叉和离地后的控制降级是当前工程急需的实机经验。
- 1 ms 控制周期、速度估计和电机输出链已经形成闭环。

缺点：

- 六状态模型与当前十维模型不同，不能直接复制控制律。
- K 只使用平均腿长，不能表达左右腿长不一致对整车模型的影响。
- 机械尺寸、零位、PID、轮电流换算和补偿参数大量硬编码。
- 全局变量较多，模式、保护和补偿相互耦合。
- 初始化和状态切换中存在阻塞等待电机上线的循环。
- 部分历史代码、死分支和重复逻辑仍保留。
- `JointToLeg()` 已在源码注释中标明存在未初始化变量导致输出为零的问题；主控制使用的是另一个 MATLAB 生成的 `leg_position()`，移植时不能误用旧函数。
- KNN 输入说明、实际特征顺序和训练数据强绑定，原模型不能迁移到另一台车。
- 支撑力、跳跃、气弹簧、上台阶和功率控制中含大量原车经验常数。

## 10. 当前工程距离实机平衡还缺什么

### 10.1 P0：任何非零输出前必须完成

1. 测量并填写实际 `link1~link4`、前后髋轴距离、轮半径和半轮距。
2. 统一实机固件、MATLAB 和 MuJoCo 中的机械参数及单位。
3. 获取本车质量、各部分质心和转动惯量，重新生成十维 A、B、K。
4. K 的采样和验证范围必须完整覆盖 `0.15~0.35 m`，不得依靠 `0.30~0.35 m` 外推。
5. 统一控制任务、PID、Kalman 和积分使用的实际 `dt`。
6. 标定四个关节电机的 CAN 映射、机械零位、反馈角正方向和力矩正方向。
7. 标定左右轮反馈方向、轮力矩方向和 M3508 力矩到电流换算。
8. 明确 DM 电机控制模式、使能成功条件和安全退出方式。
9. 设置第一阶段允许的极小关节力矩、轮电流和机械角范围。
10. 接入真实的 `chassis_set_enable()` 和 `chassis_set_mode()` 调用来源。

完成标准：输出仍保持关闭时，所有状态、K、支撑力和待发送命令方向正确；手动开启单个电机的小输出后，实际运动方向与模型完全一致。

### 10.2 P1：板凳和低力矩联调阶段

1. 单独验证左前、左后、右前、右后四个关节力矩方向。
2. 单独验证左轮和右轮正电流方向。
3. 手动活动双腿，核对 `phi1~phi4`、腿长、腿角及速度连续性。
4. 检查五连杆整个工作区的交点分支和奇异位置。
5. 验证十维状态的正方向与离线模型一致。
6. 先关闭 LQR，只调腿长 PID、基础支撑和 roll 补偿。
7. 加入机械角度软限位、电机温度、电流和异常反馈保护。
8. 模式退出时清除腿长 PID、roll PID、Kalman 和目标积分中的旧状态。
9. 检查 CAN BusOff、Tx FIFO 丢帧和恢复计数；当前发送驱动会丢弃失败帧并继续，但没有完整记录 HAL 发送失败次数。

完成标准：板凳或悬挂状态下可以稳定保持目标腿长，不出现关节顶死、方向错误、持续饱和或旧命令残留。

### 10.3 P2：固定腿长站立和平移阶段

1. 固定 `0.25 m` 腿长，使用对应实机 K 进行低增益站立。
2. 实现速度、yaw 角速度、腿长和 roll 的目标更新。
3. 目标速度和 yaw 角速度加入斜坡限制。
4. 目标位移和目标 yaw 由目标速度积分生成。
5. 上电进入站立时把目标 yaw 对齐当前 yaw。
6. yaw 使用最短角度误差，避免跨越 `+pi/-pi` 突变。
7. 修正位移积分 `0.1 m/s` 清零规则。
8. 参考 MuJoCo 加入“支撑优先、腿摆使用剩余关节能力”的分配。
9. 腿长偏差较大时削弱 LQR 腿摆控制。
10. 实现自动倒地判据和可靠的零输出退化路径。
11. 加入双腿协同 PD，防止左右腿向相反方向劈开。
12. 加入轮电机功率限制，至少先实现保平衡优先的输出缩放。

完成标准：固定腿长下能完成静止站立、小速度前后移动和低速转向，所有输出保持在持续工作限幅内。

### 10.4 P3：实时变腿长和模式完善

1. 固定腿长稳定后，把 K 输入从固定 `0.25 m` 切换为实时左右腿长。
2. 在 `0.15~0.35 m` 内分段扩大腿长工作范围。
3. 实现真正的板凳模式，作为腿部几何和位置控制调试入口。
4. 实现倒地、调整腿姿、重新站立状态机，并增加超时和人工急停。
5. 估算左右支持力，形成带滤波、持续时间和滞回的接地判断。
6. 离地时清零或降低轮输出，并限制与地面接触相关的 LQR 状态反馈。
7. 落地后逐步恢复支撑和 LQR 权重，避免瞬间冲击。

完成标准：不同腿长下 K 和输出连续，模式切换不带旧积分，离地和落地不会导致轮子高速空转或关节冲击。

### 10.5 P4：高级功能

以下功能只能在基础站立、移动和接地判断稳定后增加：

- 主动跳跃和软着陆。
- 上台阶和越障。
- 打滑检测和速度观测器降权。
- 高速转弯离心力补偿。
- 气弹簧补偿。
- 更完整的功率分配和超级电容管理。

SPR 的 KNN 离地检测不能直接复制。若决定使用 KNN，必须使用本车传感器、状态定义、采样周期和动作重新采集数据、训练、交叉验证并测试误判率。基础阶段优先使用支持力、腿长加速度和轮速差构成可解释检测器。

## 11. 推荐的复现路线

### 11.1 保留当前工程的内容

- 十维整车状态顺序。
- 四路 LQR 输出定义。
- 左右腿长二维 `poly22` K。
- 现有 `chassis_config.c` 参数集中方式。
- 全局 `chassis` 和 `imuTaskDebugState` 调试入口。
- 当前 IMU 温控、零偏、四元数 EKF 和右手系输出。
- 当前 CAN 失败帧不重发、下一周期发送最新命令的策略。

### 11.2 从 MuJoCo 复现到实机的内容

- 速度和 yaw 角速度目标斜坡。
- 目标位移和目标 yaw 积分。
- yaw 最短角度误差。
- 模式切换时 PID 和估计状态复位。
- 支撑优先的 VMC 力矩分配。
- 腿长误差较大时对腿摆控制降权。
- 十维状态、K、LQR 和 VMC 的离线回归测试。

### 11.3 从 SPR 参考实现流程的内容

- `设置模式 -> 更新反馈 -> 设置目标 -> 控制循环 -> 输出命令` 的主流程。
- 站立、零力矩、板凳、倒地和恢复的模式边界。
- 双腿协同 PD 防劈叉。
- 支持力估算和离地后的控制降级。
- 轮电机功率预测及保平衡优先思想。
- 跳跃、落地和上台阶的状态机组织方式。

### 11.4 明确禁止直接复制的内容

- SPR 的 `K[12]` 和 `LQR_K()` 系数。
- SPR 的腿长、roll、角度和速度 PID 参数。
- SPR 的四关节机械零位和正负号。
- SPR 的 M3508 电流换算和最大输出。
- SPR 的 KNN 模型、均值、方差和训练数据。
- SPR 的气弹簧、离心力、跳跃力、支持力阈值和功率经验常数。
- MuJoCo 的轮半径、质量、惯量、摩擦和执行器限幅。

## 12. 最终判断

当前代码的数学架构比 SPR 更接近完整整车建模，不能因为 SPR 代码更短就把十维模型改回六维。SPR 的价值主要在实机流程完整性，不在于其参数或模型可以直接替代当前工程。

当前工程已经具备继续上车调试的基础，但还不具备直接开启平衡输出的条件。最先要解决的不是跳跃、KNN 或上台阶，而是：

```text
实际机械参数
    -> 电机零位和极性
        -> 实机十维 K
            -> 支撑与输出限幅
                -> 固定腿长低力矩站立
                    -> 目标生成与移动
                        -> 实时变腿长
                            -> 离地、功率和其他模式
```

严格按上述顺序推进，可以最大程度利用 MuJoCo 已验证的十维控制逻辑和 SPR 已验证的实机工程经验，同时避免把另一台车的经验参数直接带入本车。

## 13. 主要源码依据

当前工程：

- `User_File/5_Task/chassis_task.c`
- `User_File/3_Chariot/1_Module/Chassis/chassis_control.c`
- `User_File/3_Chariot/1_Module/Chassis/chassis_config.c`
- `User_File/3_Chariot/1_Module/Chassis/chassis_vmc.c`
- `User_File/5_Task/task_imu.c`
- `User_File/1_Middleware/1_Driver/FDCAN/driver_fdcan.c`
- `E:\AWL_WORK\Wheel-legged\Matlab\ABK_LQR.m`

MuJoCo：

- `Controller/main.cpp`
- `Controller/controller.cpp`
- `Controller/lqr_gain.cpp`
- `Controller/vmc.cpp`
- `Controller/controller_tests.cpp`
- `OffLine_para/Para/LQR_K.py`
- `OffLine_para/Para/robot_para.py`
- `Model/robot.xml`

SPR：

- `Task/chassis_task.c`
- `Task/INS_task.c`
- `application/chassis_behaviour.c`
- `algorithm/LQR_K.c`
- `algorithm/leg_position.c`
- `algorithm/leg_speed.c`
- `algorithm/leg_force.c`
- `algorithm/kalman_filter.c`
- `algorithm/KNN.c`
